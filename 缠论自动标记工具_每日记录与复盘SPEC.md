# 缠论自动标记工具｜每日记录与复盘 SPEC

版本：0.1，2026-09-13。状态：纯规格，尚无一行代码落地，不代表数据更新、日志记录或周报生成已实现。主规格：[缠论自动标记工具_规则与算法SPEC](缠论自动标记工具_规则与算法SPEC.md)（本文档在其4.2节标记记录契约、13节验收体系、E-D03/E-D04、E-T01/E-T02基础上展开，不重复定义，只在需要处引用）。

## 0. 命名空间说明：为什么没有 O 级条目

主SPEC的 O/E/S/P 体系里，O 级专指缠中说禅108课原文的定义或明确说明。每日更新、结论冻结、周度复盘这一整套流程，原文从未讨论过——它是工程约定与评估方法，不是缠论结构定义的一部分。本文档因此**不设 O 级条目**；把工程约定安上 O 级编号等于伪造出处，不做。

规则前缀改用 `J`（journal）命名空间，避免与主SPEC已占用的 E-D / E-T / E-L 等前缀冲突：

- `E-J**`：工程约定（数据更新、日志格式、触发方式）。
- `S-J**`：评估方法（复盘口径、统计方式）。
- `P-J**`：尚待验证或明确承认为拍脑袋定的参数。

## 1. 目标与完成边界

做什么：每个交易日收盘后自动更新数据、跑引擎、把当日结论冻结成日志；每周生成复盘报告。

明确不做什么：

- 本系统不产生交易建议，不判断买卖时机，不替用户决策。
- 不声称能评估策略或引擎的盈利能力；产出的任何数字都不是收益率承诺。
- 第一年（样本量所限,见第6节）的复盘产出不具备统计推断效力,只能用于发现引擎缺陷和积累人工标注案例,不能读成"准确率"或"胜率"。

本文档全部内容目前均为未实现规格,不代表数据结构重构、定时任务、日志格式或周报生成已经存在。

## 2. "准确度"的三层拆分

"准确度"这个词在讨论中经常被混用,实际对应三个完全不同的问题,必须分开记、分开算、分开报告,不允许互相顶替。

| 层次 | 回答的问题 | 度量什么 | 不是什么 |
|---|---|---|---|
| 第一层 结构自洽性 | 上周标注的结构,本周引擎是否仍然这么标 | 标注稳定性:结构修订次数 | 不是预测力;在"当时可见"框架下修订是合法行为(参见主SPEC E-T03、P-Z08),不称为"错误次数" |
| 第二层 信号存活 | 信号是否被引擎自己定义的失效线击穿 | 引擎自画的失效线有没有破 | 不是市场对错的判断,只是引擎内部一致性 |
| 第三层 预测力 | 信号出现后价格实际怎么走 | 独立于引擎的价格结果,需独立标准 | 不能用引擎自己的规则当裁判 |

三层各自的度量方法见第5节 S-J01/S-J02/S-J03。三层的数字不得混用,任何报告展示某一层的结果时必须注明层级。

**已发生过的真实教训(必须写进文档,作为长期警示)**:v0.4升级到v0.5后,主SPEC记录的信号状态分布从7个confirmed/8个failed变为10个confirmed/4个failed,表面看像准确率翻倍。但这只是第二层的变化——`leaveDir`方向判定修复后,"回踩越界即failed"这条判定式本身触发的次数变少,是同义反复,与信号是否真的有预测力(第三层)无关。**第二层的数字若被当成第三层来读,就会得到这种虚假结论。**本文档存在的核心目的之一,就是防止这类混淆在日常复盘中重演。

## 3. 数据更新(E-J条目)

**E-J01 数据源固定**:沿用 `chan-data.js` 文件头注释记录的数据源与参数——腾讯 `fqkline` 接口,`sh600036,day,,,900,qfq`,日线,前复权。更新流程不得中途更换数据源或复权口径,否则历史价格不可比。若确需更换,视为新数据版本,按 E-J03 处理,不得静默切换。

**E-J02 更新判据**:每次拉取后,比较"数据源返回的最后一根K线日期"与"本地最后一根K线日期"。没有新增数据就直接退出,不写任何日志——不写"今日无变化"的占位记录。不维护独立的节假日/交易日历表(容易过期、需要持续维护),数据源是否给出新K线是唯一判据。对应主SPEC E-D03:缺数据不等于停牌,不自动补平盘。

**E-J03 前复权漂移**:前复权价格在每次除权(分红、送股)后会被数据源整体重算,意味着历史K线的价格本身会变。因此每次拉取必须比对历史段价格是否与本地已存数据一致:

- 一致:正常追加新K线。
- 不一致:说明发生了除权或数据源修正,不得静默覆盖历史数据。应生成新的 `data_version`,并在日志中写一条 `data_revision` 事件记录变更范围。已记录的历史结论保持原样、不回填——它们是在旧数据版本下做出的判断,回填等于用未来信息篡改历史记录。对应主SPEC E-D04。

**E-J04 数据结构重构(动手前的前置改造)**:现状是641根K线硬编码写在 `chan-data.js` 里,每日重写整个源文件不可接受,也无法追加。改造方向:数据迁到 `data/<symbol>.json` 存纯数据,`chan-data.js` 改为薄loader读取该文件。**约束:必须保持 `ChanData.dailyCandles` 这一现有调用接口的形状不变**,否则会同时破坏正式版工具、练习版工具、Mac工作台三处调用方。这是后续所有每日更新工作的前置条件,本文档第7节列为阶段A。

**E-J05 触发方式**:推荐 macOS launchd 本地定时任务。理由:数据源是国内接口,海外云端runner大概率拉取不稳定或被限流;项目目前是纯本地文件形态,不必为了定时任务引入云端依赖。代价:执行定时任务要求电脑在收盘后保持开机,这是需要用户确认的决策点(见第9节)。

## 4. 日志记录(E-J条目)

采用**每日快照 + 事件流**两种记录并存:快照回答"那天看到的是什么",事件流回答"什么时候改的主意"。事件流是第一层复盘(结构自洽性)的数据来源。

**E-J06 必须当场记录的字段(事后无法重建)**:

- `as_of`:观察日,取最后一根已收盘K线的日期。
- `data_version`
- `rule_version`:取 `ChanAlgo.RULE_VERSION`。
- 当时结论的状态:`candidate` 或 `confirmed`。
- 任何人工批注。

`rule_version` 尤其关键:引擎刚从v0.4升级到v0.5,跨版本记录不可直接合并比较,否则分不清变化来自市场行情还是来自引擎规则改动。

**E-J07 可以事后重算的**:引擎是确定性纯函数,同一数据前缀必得同一结果。结构本身、中枢边界、信号位置、力度数值都属于此类,不需要当场记录。因此日志不必做成庞大数据库,核心是"版本三元组(`data_version`/`rule_version`/`as_of`)+ 当日结论快照",结构细节需要时用该前缀重算得到,不必存一份冗余拷贝。

**E-J08 信号的两个时间必须分别记录**:

- `theory_date`:理论锚点,例如三买是回踩最低点那一天。
- `confirm_date`:确认时刻,即回踩走完、分型坐实之后。

对应主SPEC E-T01/E-T02。**复盘计算收益必须使用 `confirm_date` 对应的价格**,使用 `theory_date` 的价格等同于使用了确认时刻才能知道的未来信息。

**E-J09 只追加不改写**:日志采用 append-only 的 JSONL 格式。引擎升级导致旧结论发生变化时,新写一条 `superseded` 记录,原记录原样保留、不删除、不覆盖。对应主SPEC 4.2节的状态机与`supersedes`字段。

### 4.1 每日快照字段清单

| 字段 | 说明 |
|---|---|
| `as_of` | 观察日(最后一根已收盘K线日期) |
| `data_version` | 数据版本号 |
| `rule_version` | 引擎规则版本号 |
| `symbol` | 标的代码 |
| `structure_summary` | 当日结构摘要(笔/线段/中枢计数等,可事后重算,快照仅存指针或简要值) |
| `open_signals` | 当日仍有效的信号列表及其状态 |
| `manual_note` | 人工批注,可为空 |

### 4.2 信号事件字段清单

| 字段 | 说明 |
|---|---|
| `event_type` | `created \| confirmed \| failed \| withdrawn \| superseded` |
| `signal_id` | 对应信号对象id |
| `from_status` | 变更前状态 |
| `to_status` | 变更后状态 |
| `theory_date` | 理论锚点日期 |
| `confirm_date` | 确认时刻日期(若尚未确认为空) |
| `rejection_reasons` | 引擎给出的拒绝/失效原因 |
| `data_version` / `rule_version` | 触发本次事件时的版本三元组 |

### 4.3 建议目录结构

```text
data/<symbol>.json          # 纯行情数据,chan-data.js 改为薄loader读取
log/<symbol>.jsonl          # 事件流,append-only
review/<symbol>-<年>-W<周>.md   # 周度复盘报告
```

## 5. 周度复盘口径(S-J条目)

**S-J01 第一层 结构自洽性**:统计本周内——

- signal状态变更次数,按 `from_status→to_status` 分类计数;
- 发生 `(zd, zg, leaveDir)` 三元组变化的中枢数量;
- 被标记 `superseded` 的结论条数。

**S-J02 第二层 信号存活**:上周末仍有效(未failed、未withdrawn)的信号,本周末还剩多少。**必须按信号类型分组统计**(1B/2B/3B/1S/2S/3S性质完全不同,不可混算成一个总存活率)。

**S-J03 第三层 预测力**:

- 以确认时刻(`confirm_date`)收盘价 P0 为基准,取 N ∈ {5, 10, 20} 根之后的收盘价 PN。
- 买点信号:`r = (PN - P0)/P0`;卖点信号:`r = (P0 - PN)/P0`。
- 必须同时计算基准 `b_N`:同一时间窗内"任意交易日买入并持有N根"的平均收益(卖点方向取反)。
- **报告的是超额收益 `r - b_N`,不是 r 本身**。理由:招商银行这两年从24元走到44元,闭眼买入持有也能获利,没有对照组的胜率数字不含信息量。
- 未满N根的信号标记为 `pending`,不纳入本期统计,不得用当前部分收益顶替最终收益。

**S-J04 禁止项(防自欺清单)**:

1. 禁止用引擎自己的失效线定义"预测正确"——那是第二层,不是第三层。
2. 禁止跨 `rule_version` 直接合并统计,必须分组呈现或显著标注版本差异。
3. 禁止在样本量不足时给出百分比、胜率、准确率(阈值见 P-J04)。
4. 禁止用 `theory_date` 的价格计算收益。
5. 禁止用事后重算的结果覆盖历史快照。

## 6. 样本量与阶段目标

现状:641根日K、14个笔中枢、18个信号;每周新增约5根K线,一年约新增10—15个信号。**这个量级在一年内不足以支撑任何统计推断。**

因此第一年复盘的定位应当是:

- 发现引擎缺陷与边界情况(项目已有先例:zsb6的离开段方向错误就是人工看图发现的,而非统计发现);
- 积累人工标注,建立金标准案例库——这正好补上主SPEC第13节验收体系目前缺失的部分;
- **而不是**计算准确率。

若要更快获得统计意义,增加标的数量比延长观察时间有效得多:20只股票跑一年的样本量大致相当于1只跑20年(但同期市场环境相关,样本并不完全独立,不能直接线性折算)。代价是数据源、复权处理、交易日历都需要泛化,工程量成倍增加。是否扩展标的数量属于待决策项(见第9节)。

## 7. 执行阶段与完成判据

| 阶段 | 内容 | 完成判据 | 当前状态 |
|---|---|---|---|
| A 数据结构重构 | `data/*.json` 存纯数据 + `chan-data.js` 改为薄loader,`ChanData.dailyCandles` 接口形状不变 | 正式版、练习版、Mac工作台三处调用方均不受影响,回归验证通过 | 未实现 |
| B 手动跑通一次完整流程 | 拉取→判增(E-J02)→跑引擎→写每日快照与事件 | 人工执行一次全流程,产出符合4.1/4.2字段清单的快照与事件记录 | 未实现 |
| C 定时化 | 配置macOS launchd,收盘后自动执行阶段B流程 | 连续多个交易日自动产出记录,无需人工触发 | 未实现 |
| D 周报生成 | 按第5节口径自动或半自动生成周度复盘报告 | 生成的报告字段与第5节S-J01—S-J03一一对应,禁止项(S-J04)在生成逻辑中体现 | 未实现 |
| E 展示界面 | 记录表格接入工作台时间线面板,或提供独立页面 | 用户可在界面上查看历史快照与复盘结果,而非仅能读原始JSONL | 未实现 |

## 8. 待验证项(P-J)

- **P-J01**:多标的扩展场景下,复权口径差异、交易日历差异、数据源限流行为均未验证。
- **P-J02**:基准线算法(同窗口内等权买入持有)是否为合适的对照组,需要在真实数据上检验,目前是工程约定的选择。
- **P-J03**:5/10/20根这三个观察窗口是工程拍定的数值,未经检验是否适合日线级别的缠论结构。
- **P-J04**:"样本量低于10不给百分比"中的阈值10同样是拍定值,没有统计学依据支撑。

以上四项均老实标注为"拍脑袋定的",不伪装成已验证的方法论,这与本项目一贯的行文风格一致。

## 9. 待用户决策

1. 电脑是否可以在收盘后保持常开——决定采用本地launchd定时,还是需要评估云端方案。
2. 只跟踪600036,还是扩展到多标的(涉及第6节讨论的样本量与第8节P-J01的工程代价)。
3. 记录表格的展示落点:在现有Mac工作台内新增时间线面板,还是做成独立页面。
