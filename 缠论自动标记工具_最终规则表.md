# 缠论自动标记工具｜统一规则表

版本0.5，2026-09-13。对应 [规则与算法SPEC v0.5](缠论自动标记工具_规则与算法SPEC.md)。本表为正式SPEC摘要，规则编号映射见SPEC第15节。v0.5是对v0.4引擎（`缠论自动标记工具/chan-algo.js`）离开段判定缺陷的修复补写，不是新一轮规则设计；“最终”指本轮交付统一版，不表示不可修订。

“引擎算法”列已按SPEC第0节标注为文字规格描述还是引擎实际做法；两者不一致处以“**引擎现状**”另行注明，并给出对应SPEC新增条目。

| 标记／模块 | 判定规则 | 引擎算法 | SPEC入口与状态 |
|---|---|---|---|
| R0 包含 | 相邻区间包含，按已有方向顺序处理；上取max(L)、max(H)，下取min(L)、min(H) | 逐根合并末端，保存原始来源；启动方向不明则待定 | 第5节；可进入实现 |
| R1 分型 | 处理后连续三根，中间高低同时严格高于左右为顶，反之为底 | 四条件比较；末端依赖可变时为候选，依赖稳定再确认；原始极值定位 | 第6节；可进入实现 |
| R2 笔 | 顶底交替、分型不共用K线、之间至少1根独立处理K线；中心差≥4，最少涉及7根；顶中心高点高于底中心高点 | 文字规格：维护候选端点栈、极值筛选、间隔及价格联合检查，更新后重验邻接。**引擎现状**：`findFenxing`先做同类替换/间隔淘汰，`buildBi`再对清洗后的分型单遍贪心配对，纯函数批量重算，无候选栈与撤销事件 | 第7节；P-B05/E-B06待验证；E-B07说明引擎简化 |
| R3 线段 | 至少三笔且起始三笔重叠；首尾笔同向；标准特征序列两类结束条件 | 文字规格：状态机逐笔延伸、遇候选分界再判第一/二类。**引擎现状**：`findSegmentEnd`+`buildXianduan`对笔序列做完整不重叠窗口扫描，产出case1_no_gap/case2_gap/trailing_extreme/leading_fragment四种结束方式，末段固定candidate | 第8节；P-X08待验证，E-X09说明窗口扫描算法 |
| R4 中枢 | 三个连续已完成次级别走势对象交集；ZD=max低，ZG=min高 | 文字规格：单一递归塔逐级抬高基础层。**引擎现状**：笔中枢(操作级)与线段中枢(背景级)由同一构建器各自独立跑一遍，互不复用；新增成员重叠(0.3)/假突破降级(1.5倍区宽)/种子重叠(0.15)三个门槛判定资格，不改变ZD/ZG本身的三段求交公式。**v0.5修复**：候选离开段须自身与中枢有接触才直接采信，否则回溯找真正越界的对象（下界为种子三笔第三笔，允许其兼任离开段）；`leaveDir`（终点落在中枢哪一侧）与`leaveObj.dir`（对象自身斜率）拆成两个字段，终点仍在区间内时`leaveDir=null`。已知连锁：离开归属改变会使后续中枢种子窗口整体前移一笔，边界可能随之变化 | 第9节；ZD=ZG单列边界；P-Z06待细化；E-Z07—E-Z10说明双级别与门槛；E-Z11/E-Z12说明v0.5离开段与方向修复；P-Z08记录`leaveDir`随前缀推进的修正观察 |
| R5 走势类型 | 一个中枢的已完成类型为盘整；趋势需同级中枢合法方向分离及外围关系 | 文字规格：按GG/DD及外围波动关系判断。**引擎现状**：`computeTrendSegments`只比较相邻两个中枢的ZD/ZG两个数字，未用GG/DD；v0.5起这两个数字来自按E-Z11/E-Z12修正后的中枢，比较逻辑本身未变 | **与O-Z05直接冲突（P-Z07，未解决）**；产品文案不得宣称走势已按第18/20课严格实现 |
| R6 背驰 | 先结构及比较角色合法，再检查相应新高低、力度与结束条件；A/C同向，B为连接结构 | 文字规格：结构筛选后调用R12。**引擎现状**：参照段优先取本中枢enterObj，否则回溯上一中枢同向leaveObj；要求样本≥3根、面积≥同级中位数0.5倍、比值<0.8、且价格创同向新极值(priceExtends)五者皆满足才产出背驰 | 第10节；P-DV04未解决前不发布确认背驰；E-DV05说明产出门槛 |
| R7 一买／一卖 | 对应下跌／上涨趋势的背驰结束位置 | 绑定趋势、比较对象及结束证据；失效价取离开段自身极值，不用中枢边界（避免信号一产生就落入失效区） | 第11节；依赖R6完成政策 |
| R8 二买／二卖 | 一类点后首次次级别上/下行之后的回调/反弹结束，核查价格资格 | 跟踪前置点与后续次级对象；本版二买低点严格高于一买，二卖反之；相等边界态，穿越撤销 | 第11节E-BS02；价格限制为保守工程约定，不替代完成证据 |
| R9 三买／三卖 | 次级别离开后首次次级别回试完成，核查中枢边界 | 本版三买最低>ZG、三卖最高<ZD才进入确认检查；相等边界态；越入撤销；允许二三类同点。**引擎新增**：同一中枢若已出现1B/1S背驰，非failed的3B/3S会被降级为candidate并附冲突说明，二者不并列以确认姿态呈现 | 第11节E-BS01；E-BS03说明背驰/三买卖互斥降级；严格不等号为保守工程政策 |
| R10 MACD | 原始收盘序列的12/26/9辅助指标 | EMA首值初始化；HIST=2*(DIF-DEA)；至少130根预热；面积区间(start,end] | E-DV02、E-L02；参数和预热是工程约定 |
| R11 操作参考 | 防守/压力/观察/触发/失败对应具体策略假设 | 文字规格：观察区/压力/触发/失败线分层体系。**引擎现状**：仅取最近两个笔中枢ZG/ZD为支撑压力线、最近信号的boundary为防守位，无独立观察区/触发规则字段 | S-01、S-02；E-R11a说明为精简子集，非完整交付 |
| R12 独立力度 | 同对象角色下分别观察幅度、速度、动量、效率、回撤 | 第16节全量指标（推进率/速度/收盘路径效率/回撤反弹比）**未实现**；引擎已实现的只是R6/R11所需的MACD面积比子集 | 第16节；不能因R6/R11能跑就视为R12已完整交付 |
| R13 多级别联立 | 父层确定背景与具体目标，子层提供目标内部证据；局部转向不等于父层转向 | **未实现**：引擎中未出现任何父子结构代码 | 第17节；P-M08依赖递归及结束规则完成 |

## 力度规则明细（R12）

| 指标 | 算法 | 边界 |
|---|---|---|
| 推进率 | d*(Pe-Ps)/Ps，d上涨+1/下跌-1 | Ps<=0返回价差及原因 |
| 推进速度 | 推进率/交易分钟 | 固定交易日历，不跨周期比较每根速度 |
| MACD面积 | 上Σmax(HIST,0)，下Σmax(-HIST,0) | 同周期同参数；前面积0不可比；未完成后段标partial |
| MACD峰值 | 对应方向柱绝对值最大值 | 无该方向柱为0，无数据为null |
| 收盘路径效率 | abs(末收盘-首收盘)/Σabs(相邻收盘差) | 不混用影线锚点；分母0或不足两点为null |
| 后续回撤/反弹 | 上max(0,Pe-后续最低)/abs(Pe-Ps)，下镜像 | 从端点下一根开始，仅至as_of指定首次反向对象；无数据为null；允许>1 |
| 相对变化 | 后值/前值及差值 | 前值<=0不算常规比值；浮点容差按E-L04；指标冲突显示分歧 |

第15课的均线面积力度口径本版仅列参考，尚未实现。以上度量不声称是唯一的原典力度，也不能独立确认背驰。

## 多级别联立明细（R13）

| 检查 | 规则／结果 |
|---|---|
| 周期与级别 | 日线/分钟线是数据周期，父子层级由结构构造；同周期看多不是区间套 |
| 父子归属 | 保存parent_id/child_ids/role，单纯时间重合不够 |
| 目标任务 | 区分末段结束、二类回调结束、三类首次回试结束等 |
| WAITING | 目标资格存在，内部证据不足 |
| LOCAL_SIGNAL | 小级别证据存在，父层目标尚未全部满足 |
| TARGET_SATISFIED | 该目标全部条件与完成证据满足；不自动宣布大级别上涨/下跌 |
| INVALIDATED | 目标明确前提失效；不自动宣布反向趋势 |
| UNMAPPED/DATA_INSUFFICIENT | 无法确定归属或缺少真实小级别数据，不生成确认 |
| 区间套 | 逐层检查外层候选的内部结构；每层有比较依据，外层前提失效则撤销外层结论 |

## 共同约束

固定数据与复权版本；保留价格精度；候选/确认分开；理论位置/实际可知时间/策略触发时间分开；不使用未来数据；不伪造小周期行情；绘图共享真实时间和价格坐标；同一前缀重算与逐根回放一致。

各模块按依赖事件更新，R0—R13不是一次性串行流程。待验证内容及新增17项验收见SPEC第18节；已有20个基础JSON案例不代表新增模块已通过测试。

## v0.4→v0.5 引擎实现要点（更新）

- 已接入真实数据：招商银行(600036)，2024-01-19至2026-09-11，腾讯qfq前复权日K，见 `chan-data.js`。
- 已有可交互网页图层与详情面板：`缠论标记工具.dc.html`（依赖 `support.js` 提供的Design Canvas运行时渲染，不能当纯静态HTML直接打开查看效果）。
- 中枢为双级别并存（笔中枢=操作级、线段中枢=背景级），不是单一递归塔；三个重叠比例门槛（成员0.3、假突破1.5倍区宽、种子0.15）用于中枢内部资格判定，与v0.2废止的50%阈值用途不同。
- **v0.5修复了离开段判定的两处缺陷**（E-Z11/E-Z12）：候选离开段不再仅凭“自身与中枢毫无接触”就直接采信，而是回溯找真正与中枢有接触且端点越界的对象（下界为种子三笔第三笔，允许其兼任离开段）；`leaveDir`（终点落在中枢哪一侧）与`leaveObj.dir`（对象自身斜率）拆成两个字段，不再混用。全历史回放对比：笔中枢总数不变（14个）；R7—R9信号中3处出现方向反号（均为此前`failed`候选变为方向相反的`confirmed`）；R7—R9信号状态分布从7个`confirmed`/8个`failed`变为10个`confirmed`/4个`failed`——**这不是准确率提升的证据**，只是方向判对后“回踩越界即failed”这条判定式触发次数变少的同义反复，信号是否有预测力仍需按第13节做真实前缀回放统计，本版未做。
- 已知连锁：离开归属修正会使受影响中枢之后的种子窗口整体前移一笔，历史中枢边界可能随版本升级变化，属于E-Z11的必然推论而非新缺陷；受影响的既有确认结果按第4.2节标`superseded`，不做静默覆盖。
- 前缀回放观察（P-Z08）：对130~640共511个前缀逐一重算，发现4处同一中枢的`leaveDir`随K线推进发生方向修正；每次修正在“当时可见”框架下合法，但说明同一结构对象的方向标注在不同`as_of`下可能不一致，尚缺专项测试覆盖。
- **已知未解决冲突**：走势类型判定（R5）目前只比较相邻中枢ZD/ZG，未按O-Z05使用GG/DD和外围波动关系，与SPEC文字直接冲突（P-Z07）。
- R12独立力度、R13多级别联立仍主要是规格文本，未按第16、17节全量实现；不要把“背驰/买卖点能跑出结果”等同于这两个模块已交付。
- 详细偏差清单见 [规则与算法SPEC v0.5 第0节](缠论自动标记工具_规则与算法SPEC.md)。
