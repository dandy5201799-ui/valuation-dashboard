# 投资研究体系｜前端估值仪表盘项目总览

版本日期：2026-08-03

## 一、现在这个项目的定位

这个目录现在只保留“能让手机端估值页面继续运行和迭代”的核心内容。

当前正式链路是：

```text
行情/市场估值数据
        ↓
SQLite 本地数据库
        ↓
data/*.json
        ↓
估值仪表盘.html
```

Excel 工作簿和 Word 研究卡已经不再是日常运行链路的一部分。它们作为历史底稿归档保存；后续不再同步 WPS 云文档。

## 二、核心文件

| 文件/目录 | 定位 | 是否日常使用 |
|---|---|---|
| `估值仪表盘.html` | 手机端估值页面主文件 | 是 |
| `data/stocks.json` | 三只股票估值页面数据 | 是 |
| `data/market_temperature.json` | A股、港股、美股市场估值温度数据 | 是 |
| `data/market_temperature.meta.json` | 市场温度数据来源、告警和新鲜度说明 | 是 |
| `05_数据仓库/investment_research.sqlite` | 本地真实数据、估值输入、试算结果和历史记录 | 是 |
| `05_数据仓库/schema.sql` | 数据库结构定义 | 需要维护 |
| `05_数据仓库/README.md` | 数据仓库使用说明 | 需要维护 |
| `SPEC.md` | 给 Codex / Claude Code 看的项目规则与模型说明 | 需要维护 |

## 三、保留在项目根外的运行脚本

脚本位于本项目父目录：

| 脚本 | 做什么 | 是否保留 |
|---|---|---|
| `update_stock_valuations.py` | 更新贵州茅台、招商银行A、胜宏科技H的最新行情，写入 SQLite，并重建 `stocks.json` | 保留 |
| `update_market_temperature.py` | 更新 A股、港股、美股市场估值温度，写入 SQLite，并重建市场 JSON | 保留 |
| `build_mobile_dashboard.py` | 从 SQLite 生成前端 JSON，并把 fallback 数据写回 HTML | 保留 |

除此之外，旧的 Excel/Word 生成脚本、WPS 同步脚本、迁移脚本都可以归档。日常不再使用。

## 四、日常怎么更新

### 每个交易日

更新个股估值页面数据：

```bash
python3 ../update_stock_valuations.py
```

更新市场估值温度：

```bash
python3 ../update_market_temperature.py
```

如果只是重新生成前端 JSON：

```bash
python3 ../build_mobile_dashboard.py
```

### 每季度财报后

季度财报更新不是简单改价格，而是要重新判断估值输入：

1. 把新的财报事实写入数据库；
2. 判断 EPS、BVPS、ROE、利润率、现金流等关键参数是否改变；
3. 必要时新增一次 `valuation_runs`；
4. 同步写入 `valuation_inputs`、`scenario_results`、`profit_basis_tiers`；
5. 再运行 `build_mobile_dashboard.py` 刷新页面。

## 五、当前三只股票的估值展示逻辑

| 股票 | 实用合理区间 | 严格模型区间 | 当前判断核心 |
|---|---|---|---|
| 贵州茅台 | EPS × 实用PE区间 | 两阶段 FCFE，总回报模型 | 增长中枢与终局PE |
| 招商银行A | BVPS × 实用PB区间 | 两阶段剩余收益模型 | ROE路径、股息率、PB |
| 胜宏科技H | 多口径利润 × PE区间 | 五年利润路径＋退出倍数/H模型 | 2025盈利跃升是结构性还是周期性 |

页面上优先展示“价格落在区间的什么位置”，而不是展示完整工作簿。

## 六、归档原则

已经归档或即将归档的内容包括：

- 四本 Excel 工作簿；
- 两份 Word 公司研究卡；
- 旧版设计原型；
- 历史输出、预览图、中间数据；
- 只服务 Excel/Word/WPS 的旧脚本。

归档是移动保存，不删除。以后如果需要复查历史模型，可以到归档目录找；但日常运行以 SQLite 和前端 JSON 为准。
