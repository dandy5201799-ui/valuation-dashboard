# 缠论自动标记工具｜规则与算法 SPEC

版本：0.5，2026-09-13。状态：统一规则基线；尚非完成的自动标记软件。配套摘要：[最终规则表](缠论自动标记工具_最终规则表.md)。
每日更新、结论记录与周度复盘（不涉及标记规则本身）见 [每日记录与复盘SPEC](缠论自动标记工具_每日记录与复盘SPEC.md)（J命名空间，无O级条目）。

本文件是正式规则依据，配套表只作同版本摘要。O/E/S/P分别表示原文规则、工程约定、策略扩展、待验证项。规则统一不代表原文所有边界已形式化或引擎已验证。

v0.2新增独立力度与多级别联立模块，固定保守边界政策，统一模块编号R0—R13，增加专项验收矩阵。废止聊天旧表的50%重叠阈值、强行兜底确认线段及“A/B/C全部同向”等表述。

v0.4是对 `缠论自动标记工具/chan-algo.js`（`RULE_VERSION = chan_spec_v0.4_dual_level`）已落地引擎的一次对齐补写，不是新一轮规则设计。做法是：先读引擎源码确定它实际在算什么，再把和v0.2文字不一致或v0.2未覆盖的地方，按O/E/S/P规则如实记入第0节和相应章节；引擎里明确偏离原有O级规则文字的地方（例如走势判定），标记为待解决偏差而不是直接改写O级规则去迁就代码。本次更新未重新逐图核对65/71/77/78/81课，第2节来源表保持v0.2状态。

v0.5（`RULE_VERSION = chan_spec_v0.5_leave_side`）修复`buildZhongshuFromObjects`离开段判定的两处工程缺陷，新增E-Z11、E-Z12，是对v0.4实现的缺陷修复而非规则重新设计：（1）此前候选离开段一旦自身与中枢区间`!overlaps`（毫无接触）就直接采信为离开段，未区分"这一段冲出了中枢"与"这一段开始之前价格就已经在中枢外面晃"两种情形；v0.5改为回溯，找真正与中枢有接触且端点越界的对象作离开段，下界为种子三笔第三笔（即允许第三笔兼任离开段）。（2）此前`leaveDir`直接取`leaveObj.dir`（对象自身涨跌斜率），会把"整体在中枢一侧、自身斜率朝另一方向"的笔误判方向；v0.5把`leaveDir`（终点落在中枢哪一侧）与`leaveObj.dir`（对象自身斜率）拆成两个概念，`leaveDir`按终点相对`[zd,zg]`重新计算，终点仍落在区间内时`leaveDir=null`，不编造方向。下游R5/R6/R7—R9凡是判断"离开方向"的地方，全部改用`leaveDir`。这一修复有一个必然的连锁推论：离开段既然可能回溯到种子三笔的第三笔，该笔就会同时成为下一个中枢种子窗口的首笔，导致后续中枢的窗口整体前移一笔、区间边界可能随之变化——这是规则的必然结果，不是缺陷，但意味着升级到v0.5后，部分历史中枢的区间边界、成员数会与v0.4结果不同。受影响的既有确认结果按第4.2节标`superseded`并保留原记录，不做静默覆盖或删除。

## 0. v0.5 引擎实现快照与已知偏差

本节记录 `chan-algo.js` 相对v0.2文字的实际实现方式和已发现的偏差，按模块归入下表；详细规则仍以对应正文章节及新增E/P条目为准。表中标注"v0.5新增/v0.5起"的内容为本轮离开段修复带来的变化，其余为v0.4对齐补写时的记录。

| 模块 | 引擎现状 | 与v0.2文字的关系 |
|---|---|---|
| R2 笔 | `buildBi` 对分型序列做单遍贪心相邻配对，不合格则跳到下一个分型；`findFenxing` 在分型层已完成同类替换（O-B03）与间隔淘汰。整体是纯函数批量重算，非事件增量状态机 | 效果覆盖O-B01—03主干，但不是第7节伪代码描述的“候选端点栈+撤销事件”结构；新增E-B07说明，P-B05/E-B06维持未验证 |
| R3 线段 | `findSegmentEnd`+`buildXianduan` 做完整不重叠窗口扫描：每段独立找特征序列分型，区分`case1_no_gap`/`case2_gap`/`trailing_extreme`/`leading_fragment`四种结束方式；最后一段固定`candidate` | 新增E-X09描述该算法；特殊线段（O-X06的range_low/high与端点分离）字段已建但未独立验证，P-X08仍未解决 |
| R4 中枢 | `buildZhongshuFromObjects` 对笔和线段分别构造两条独立中枢序列（笔中枢=操作级，线段中枢=背景级），不是同一递归塔上的层层复用；新增重叠比例三个门槛区分成员资格、假突破降级、种子资格。v0.5新增：候选离开段须自身与中枢有接触才可直接采信，否则回溯找真正越界的对象（下界为种子三笔第三笔，允许其兼任离开段）；`leaveDir`（终点落在中枢哪一侧）与`leaveObj.dir`（对象自身斜率）拆成两个字段，终点仍在区间内时`leaveDir=null` | 新增E-Z07—E-Z10、v0.5新增E-Z11/E-Z12；核心ZD/ZG公式仍是O-Z02三段求交，未改动。E-Z11有已知连锁：离开段回溯到种子第三笔时，该笔会同时成为下一中枢种子窗口首笔，导致后续中枢边界随之整体前移一笔，升级后历史标注可能变化 |
| R5 走势 | `computeTrendSegments` 仅比较相邻两个中枢的`zd`/`zg`两个数字判断上涨/下跌/盘整，首个中枢固定`pending`。v0.5起，这里比较的`zd`/`zg`来自已按E-Z11/E-Z12修正的中枢，边界本身可能与v0.4不同，但比较逻辑本身未变 | **与O-Z05“趋势分离检查外围波动关系，不能只比较ZD/ZG两个框”直接冲突**，记为P-Z07，未解决前不得宣称走势判定已按O-Z05实现 |
| R6 背驰 | `computeBeichi`要求参照段可比（`computeTrendStrength`）且比值<0.8且离开段价格创出相对参照对象和中枢边界的新极值(`priceExtends`)才产出候选/确认背驰标记 | 新增E-DV05描述参照段选取（enterObj同向优先，否则回溯上一中枢同向leaveObj）与中位数动能门槛；P-DV04维持未解决 |
| R7—R9 买卖点 | `deriveSignals`按中枢自带enterObj/leaveObj/pullbackObj/postPullbackObj推导，1B/1S失效线用离开段自身极值而非中枢边界；同一中枢背驰信号会把非failed的3B/3S降级为candidate | 新增E-BS03描述背驰与三买/卖同枢互斥降级规则；E-BS01/E-BS02判定条件与引擎一致 |
| R11 操作参考 | `supportResistance`/`failureLine`仅取最近两个中枢的ZG/ZD为支撑压力线、最近信号的`boundary`为防守位 | 明显简化于第11节S-01/S-02设想的观察区/压力/触发分层描述，标记为部分实现 |
| R12 独立力度 | 已实现MACD面积比（E-DV02口径），但只服务于R6/R11所需的enterObj/leaveObj比值，未实现第16节列出的推进率、推进速度、收盘路径效率、回撤/反弹比等全部独立指标 | 第16节E-L01—L07仍视为未实现规格，不能因为R6/R11能跑就认为R12已完整交付 |
| R13 多级别联立 | 未在引擎中出现任何父子结构代码 | 维持“未实现”，第17节原样有效 |
| 网页绘图 | `缠论标记工具.dc.html`+`support.js`已实现可交互K线图、分层显示、点击详情面板、真实数据（招商银行600036，2024-01-19至2026-09-11，前复权）接入 | 第12节图形与解释验收要求可据此逐条对照，但尚未做第13节要求的真实前缀回放一致性测试 |

以上标记为P的偏差（尤其P-Z07）在解决前，不得在产品文案或结论文字中宣称“走势类型已严格按第18/20课实现”。

v0.4→v0.5观测记录（如实记录，非成绩）：对同一份641根招商银行数据全量重算，R7—R9信号的状态分布从7个`confirmed`/8个`failed`变为10个`confirmed`/4个`failed`。**这不构成“引擎变准了”的证据**：`leaveDir`方向判对之后，信号自然更少被立即出现的反向价格判定为`failed`，这是同义反复——方向标对了，"回踩越界=failed"这条判定式本身触发的次数就会变少，与信号是否真的有预测力无关。信号是否有实际预测力，必须按第13节要求做真实前缀回放并统计独立命中率才能回答；本版未做这项统计，不得把上述数字读成准确率或收益的改善。

## 1. 目标与完成边界

从真实 OHLC 数据自动生成分型、笔、线段、中枢、走势类型、背驰和三类买卖点。每个标记可解释、可回放、可追溯到原始价格和确认时刻。

本阶段交付：七类标记的依赖与接口；基础四层的算法规格；规则分歧与例外清单；机器可读的基础验收样例；后续实现与发布条件。

当前没有完成：计算引擎、真实行情接入、真实案例人工基准、历史回放一致性测试、网页绘图。不能把本规格或样例验证等同于这些工作已经完成。

“严格”指规则版本固定、计算可复现、证据完整，不代表预测或交易收益的确定性。

## 2. 规则来源和优先级

每条规则标记为：

- O：原文的定义或明确说明。
- E：为使程序确定运行而制定的工程约定，不声称是原文唯一解释。
- S：策略规则，例如收盘突破压力、止损条件；不替代缠论结构定义。
- P：尚待逐图或真实数据核对，禁止悄悄采用默认答案。

采用后续澄清与更正修订早期口径。现有教学 HTML 不是算法真值来源；开源库是对照实现，不是裁判。

已核对的本地原文存档位于 `chzhshch-108/gitbook-book/108/`。在线原文已在前一轮分析核对；本阶段补充核对第65、71、77、78、81课本地文本。

| 来源 | 重点 | 本地存档 |
|---|---|---|
| [62](https://blog.sina.com.cn/s/blog_486e105c01000bf2.html) | 包含、分型、成笔间隔 | lesson-062.md |
| [65](https://blog.sina.com.cn/s/blog_486e105c01000bpo.html) | 顺序合并、方向、三笔重叠、笔破坏 | lesson-065.md |
| [67](https://blog.sina.com.cn/s/blog_486e105c01000c16.html) | 特征序列、两种结束情况 | lesson-067.md |
| [71](https://blog.sina.com.cn/s/blog_486e105c01000c8i.html) | 序列归属、假设分界、待定结构 | lesson-071.md |
| [77](https://blog.sina.com.cn/s/blog_486e105c01000cih.html) | 成笔价格条件、筛选顺序 | lesson-077.md |
| [78](https://blog.sina.com.cn/s/blog_486e105c01000cio.html) | 特殊线段、原始端点与标准化区间 | lesson-078.md |
| [81](https://blog.sina.com.cn/s/blog_486e105c01000cmz.html) | 第71课图例更正、数据精度 | lesson-081.md |
| [18](https://blog.sina.com.cn/s/blog_486e105c010007t8.html)、[20](https://blog.sina.com.cn/s/blog_486e105c010007zw.html) | 中枢、趋势、扩张、三买三卖 | lesson-018.md、lesson-020.md |
| [21](https://blog.sina.com.cn/s/blog_486e105c0100082x.html) | 三类买卖点关系 | lesson-021.md |
| [24](https://blog.sina.cn/dpool/blog/s/blog_486e105c0100087y.html)、[37](https://blog.sina.com.cn/s/blog_486e105c01000974.html) | MACD辅助与背驰结构前提 | lesson-024.md、lesson-037.md |

参照实现：[chan.py](https://github.com/Vespa314/chan.py)。若后续借鉴，须记录 commit、配置、许可与具体差异。不得仅凭与库一致就判定符合原文。

## 3. 计算层次与展示层

| 层 | 输入 | 输出及图上标记 |
|---|---|---|
| 数据 | 行情与元数据 | 原始 K 线、数据异常 |
| 包含 | 原始高低区间 | 合并区间及原始来源，可切换查看 |
| 分型 | 合并区间 | 顶/底、候选与稳定证据 |
| 笔 | 分型候选集合 | 上/下笔、候选端点、拒绝原因 |
| 线段 | 笔及其确认状态 | 上/下线段、特征序列与破坏证据 |
| 中枢 | 有明确层级的组成对象 | 核心区间、外围波动、延伸/扩张/新生 |
| 走势 | 同级中枢与连接对象 | 上涨/下跌/盘整/发展中/数据不足 |
| 力度 | 有明确边界的比较对象和价格序列 | 幅度、速度、面积、峰值、效率、回撤及分歧 |
| 背驰 | 合格比较对象、结构及力度指标 | 比较区间、力度数值、候选及确认依据 |
| 买卖点 | 结构与完成证据 | B1/B2/B3、S1/S2/S3及识别时间 |
| 多级别联立 | 父子结构、各级证据及目标假设 | 观察任务、内部证据、目标满足或失效 |
| 策略 | 结构、用户选择的策略版本 | 观察区、压力、防守、触发、失败线 |

结构层与策略层单向依赖；不得为匹配买点倒推修改笔或线段。

## 4. 数据与通用状态契约

### 4.1 数据

E-D01：价格以数据提供方精度转换为整数或十进制定点数。不得用显示四舍五入后的价格判断相等。复权价的精度单独记录，不能强行按未复权最小报价单位截断。

E-D02：每根数据保存 `symbol, source, timeframe, start_time, end_time, O,H,L,C, volume, is_closed, adjustment_version`。验证 `L<=min(O,C)<=max(O,C)<=H`、时间有序且唯一；异常数据阻止相关区间推导。

E-D03：固定时区、交易日历、午休和集合竞价归属。缺数据不等于停牌，不自动补平盘。日线 OHLC 不能反推出当日高低发生先后。

E-D04：分析起点和暖启动历史写入配置。缩放、滚动和裁剪画布不改变计算起点。换起点、修正行情或改变复权因子产生新数据版本，旧结果可追溯。

### 4.2 标记记录

所有对象至少保存：

```text
id, revision, kind, direction, timeframe, structural_level,
anchor_time, anchor_price, first_seen_at, confirmed_at,
status, rule_ids, source_ids, evidence, rejection_reasons,
data_version, rule_version, supersedes
```

`structural_level` 未建立时为 null，不能用“日线”填充未知结构级别。

状态：`candidate`（可移动）→ `confirmed`（满足本版本确认政策）；候选可 `withdrawn`。修正数据或算法导致既有确认结果变化，使用 `superseded` 及原因，不删除历史。

E-T01：位置与确认时间分离。确认时刻不得早于任一依赖对象当时可用的时间。

E-T02：`condition_met_at` 可附加记录“几何条件首次满足”。`confirmed_at` 表示依赖稳定后满足本项目保守确认政策；二者不一定相同。

E-T03：右侧发展中合并 K 线可以改变；分型几何上已经出现，不代表候选笔端点已冻结。不得用一个 confirmed 标志替代全部层次的完成关系。

## 5. 包含算法：可进入基础实现

O-K01：区间 A 包含 B 或 B 包含 A：

```text
(A.L<=B.L and A.H>=B.H) or (B.L<=A.L and B.H>=A.H)
```

E-K02：包含判定含相等边界；全等区间也合并。同价极值的所有来源保留，绘图默认选择最早来源，属于显示约定。

O-K03：顺序处理。先合并 A、B，再用合并结果与 C 比，不能一次对“互相有包含”的整组取极值。

O-K04：已有前一非包含区间 P 与末端 M 时，以 P→M 的高低方向确定合并方向。向上合并 `(max(L),max(H))`，向下合并 `(min(L),min(H))`。不以阴阳、开收盘或整段未来方向判定。

```text
push(new_bar):
  验证数据并转换为价格区间
  若与末端 M 不包含：追加；新末端形成方向证据
  若包含且方向已知：依方向更新 M，保存逐次合并日志与来源
  若包含且方向未知：进入启动待定缓存，不输出确定分型
```

E-K05：启动缓存至少保留全部原始区间；并行维护上/下两种初始方向的合法合并解释，直到出现能够建立非包含方向的区间。不能借未来数据改写过去“当时已知”。若两分支尚不能唯一收敛，返回 `insufficient_context`，从已知历史锚点重放。第一版不得强行任取一个分支。

E-K06：新增非包含区间后，前一区间不再参与与未来新区间的跨界反向合并。本约定基于顺序处理，需用连续包含测试验证。

输出合并区间，不伪造开收盘；原始蜡烛保留，合并层显示高低带或独立面板。

## 6. 分型算法：可进入基础实现

O-F01：在三个连续处理区间 P,M,N 上，顶为 M.H 同时大于左右高点且 M.L 同时大于左右低点；底为两个条件全部取小于。

E-F02：使用严格比较。只有两根、相等未排除、仍存在包含的输入，不输出确认分型。算法单元直接输入残留包含数据时报告 `unnormalized_input`。

E-F03：几何成立的分型先作为局部候选输出。项目保守冻结规则为三根依赖区间均已稳定；右侧区间尚为可合并尾部时不冻结。日内未收盘数据只用于预览。

E-F04：保存全部局部候选分型供解释；参与笔的有效分型由下一层按间隔、结合律和价格关系筛选。局部候选可以共享邻近 K 线，不意味着可以共享 K 线后成笔。

绘图：顶箭头指向中间合并区间 H 的原始来源，底指向 L 的原始来源；同时高亮三根处理区间。不能把所有箭头放在合并区间起始日。

## 7. 笔算法：主规则明确，端点冻结与复杂淘汰待完整基准验证

版本建议：`bi_62_77_conservative_v1`，不得混入缩短间隔的新笔或跳空抵根数规则。

O-B01：一底一顶构成上笔，一顶一底构成下笔；顶、底分型不共用处理后 K 线，之间至少一根独立处理后 K 线。

令中间 K 线索引为 i,j，则各分型覆盖 `[i-1,i+1]` 和 `[j-1,j+1]`，独立根数为 `j-i-3`，要求 `j-i>=4`。分型合计最少涉及七根处理 K 线；两中心含首尾有五根。两个数字不是相互矛盾。

O-B02：顶分型中心 K 线高点必须高于底分型中心 K 线高点（第77课的价格区间条件）；不能只要求顶价大于底价就放行。

O-B03：依据第77课，同类候选中后顶更高、后底更低时，替换较弱候选；相等先保留。同类连续候选直到合格异类端点出现，再确定连接所用的端点并淘汰其余。被淘汰者保留理由，不再参与主折线。

E-B04：间隔不足的异类分型保留在局部事件流，不能因为它出现就冻结上一笔。同类替换必须重新检查邻接的成笔间隔与价格条件。

```text
按时间取分型事件；维护候选端点栈与连接假设
  合格异类：建立候选连接并记录价格/间隔证据
  同类更极端：替换候选，重新验证相邻连接
  同类相等：保留顺序，待异类事件裁决
  不合格异类：记录拒绝原因，继续等待
  原连接因新极值不再有效：重算受影响候选后缀，发出撤销事件
```

P-B05：原文筛选步骤与“先排除不合格异类”的执行顺序，在嵌套短分型情形下须补充端到端案例；在完成这项验证前，不能把上述框架称为全边界完整实现。

E-B07（v0.4引擎现状）：`chan-algo.js` 的 `buildBi` 是对已清洗分型序列的单遍贪心相邻配对——顶底交替、间隔与价格条件不满足就整体跳过一格，不维护候选端点栈，也不产生显式撤销事件；同类替换与间隔淘汰在上一层 `findFenxing` 中已完成。引擎为纯函数批量重算：同一份数据从头算和逐根重放在最终结果上等价，但不提供第7节伪代码描述的“更新后重新验证受影响候选后缀”的事件流。这是当前的工程简化，不代表P-B05、E-B06已经验证通过。

E-B06：候选成笔与笔已完成分开。建议保守完成政策：稳定反向连接建立后，才冻结前笔端点；必须用反向连接继续延伸、同类替换及间隔变化测试证明冻结条件成立。未证明的后缀保持候选，不承诺固定延迟几根。

绘图：上笔连接底源价与顶源价，下笔反之；候选虚线。解释卡展示两组三 K 线、独立根数、价格条件、淘汰分型与确认事件。

## 8. 线段算法：必须实现双情况状态机及更正案例

O-X01：至少三笔；起始三笔有公共价格区间；首尾笔同方向，完成线段笔数为奇数；上段底到顶且顶高于底，下段反之。

前三笔重叠只是起始条件，不是线段已结束或三笔必然分段的依据。

O-X02：上段采用下笔作为特征元素，下段采用上笔；元素为价格区间并保存来源笔。相邻特征区间交集为空为特征缺口，不等于 K 线跳空。

O-X03：第67课两类结束证据：

- 第一类：标准特征序列分型第一、第二元素无缺口，按相应顶/底分型判断分界。
- 第二类：有缺口；从候选端点开始检查反向结构的第二特征序列，出现相应反向分型才满足结束证据。第二特征序列的分型不再递归区分这两类。

O-X04：结合第71、78课：合并前必须知道序列归属。在假设分界前后，不能把不同性质元素无条件合并。第二类的第二特征序列必须正常处理包含，不能套用第一类分界处的例外。

O-X05：单笔破坏不等于线段破坏；反向三笔没有形成相应结构，或原方向恢复，可能仍是原线段延续。

状态机：

```text
SEEK_SEED → EXTENDING
EXTENDING → TEST_BOUNDARY（发生候选特征分型/破坏）
TEST_BOUNDARY → CASE1_CHECK / CASE2_WAIT
CASE1_CHECK → CONFIRMED_END（全条件满足）或 EXTENDING/待定
CASE2_WAIT → CONFIRMED_END（第二序列分型满足）或 EXTENDING/待定
```

每个候选分界保存 `left_feature, boundary_pen, right_features, gap, ownership, second_sequence, dependency_status`。任何确认必须展示是哪一类以及哪几笔完成了证据。

O-X06：第78课明确存在内部极值不在结构端点上的特殊线段。因此必须保存两组数据：

- `start_anchor/end_anchor`：结构分界点，用于主图连接。
- `range_low/range_high` 与各自来源：整段实际价格范围，用于上层区间分析。

不得硬加“线段端点必须是整段绝对极值”的不变量。标准化仅作为上层对象视图，不回写或挪动主图原始端点。

O-X07：第81课更正优先于第71课旧图。在对应构型中，高点7不低于5与7低于5会导致不同分段；必须转录原图全部端点，分别制成数值案例，不能只用5和7两个价格猜全图答案。

P-X08：本阶段尚未把第71、78、81课全部图例转换成数值基准，也未完成特征包含的全分支伪代码。线段发布门槛仍未满足。不能用“跌破前低”替代这里的缺项。

E-X09（v0.4引擎现状）：`findSegmentEnd`+`buildXianduan` 把整条笔序列一次性划分成完整不重叠的线段窗口：从当前起点笔向后扩窗，逐步收集反向特征序列并调用 `featureFractal` 找顶/底分型，命中后按有无缺口分别标 `case1_no_gap`（第一类，直接采信）或 `case2_gap`（第二类，还要求下一段线段笔数≥3才算稳，否则记 `gap_case_next_segment_too_short` 保持candidate）；扫不到分型的收尾段记 `trailing_extreme`（按方向取极值兜底，仍标candidate）；数据起点的残段记 `leading_fragment`。最后一段无论哪种情形固定 `candidate`/`segment_still_forming`，对应E-T03“右侧未验证不冒充完成”。`range_low/range_high`字段已按O-X06落地保存（见`buildXianduan`里的`lows/highs`），但尚未做“标准端点 vs 整段极值”分离生效的独立验证案例，P-X08实质仍未解决。

## 9. 中枢与走势：先固定递归接口

O-Z01：严格中枢输入是连续、已完成的次级别走势类型。最底层采用线段构造时必须声明基础层约定。笔中枢可作为独立实验模式，不与递归中枢共享标签。

O-Z02：三个合格对象区间求交，`ZD=max(L1,L2,L3)`、`ZG=min(H1,H2,H3)`。`ZD<ZG` 为正宽重叠；`ZD>ZG` 无交集。E-Z03：`ZD=ZG` 单独输出 `boundary_touch`，本版不自动提升为有效中枢，标明保守工程约定。

O-Z04：核心区间由初始对象确定；延伸另外增加组成对象和外围范围。必须记录上移/下移新生与扩张关系，不能每次重算所有组成对象交集而悄悄收窄原核心。

O-Z05：按第20课定义计算 Z 走势对象的 `GG,DD`；不能未核对对象归属就拿任意全窗口最高最低替代。趋势分离检查外围波动关系，不能只比较 ZD/ZG 两个框。

O-T01：已完成类型：一个相应级别中枢对应盘整；趋势依赖至少两个同级中枢及合法方向分离。另设 `developing/unknown`；零中枢不强制判盘整。

P-Z06：基础递归种子、走势完成、连接段归属、延伸转扩张、等价分解选取仍须专门算法审计。不得直接递归“三段求交”就声称完成严格走势引擎。

E-Z07（v0.4引擎现状：双级别中枢）：`buildAllZhongshu` 对笔序列和已确认线段序列分别调用同一个 `buildZhongshuFromObjects`，产出`bi`（笔中枢，操作级）与`xianduan`（线段中枢，背景级）两条独立中枢队列，互不复用彼此的组成对象，也不是O-Z01设想的“同一递归塔逐级抬高基础层”。这是本版对O-Z01“笔中枢可作为独立实验模式，不与递归中枢共享标签”的具体落地方式：两个级别各自完整跑一遍中枢/离开/买卖点逻辑，UI上分别展示，不互相染色。

E-Z08（成员重叠门槛，非ZD/ZG计算）：`buildZhongshuFromObjects` 引入 `overlapRatio(o, zd, zg)`——对象与`[zd,zg]`交集占自身幅度的比例——用于判定一根笔/段是否有资格算“中枢内成员”而非直接判定中枢边界本身。成员门槛`ZS_MEMBER_MIN_OVERLAP=0.3`：低于此值即便端点或区间与中枢仍有交集，也不算合格成员，防止“幅度数倍于区宽、直接穿越”的段被中枢吸收成大口袋。此门槛与v0.2废止的“50%重叠阈值”不同：后者曾被误用于ZD/ZG本身的判定，本门槛只管成员资格、不改变O-Z02的三段求交公式。

E-Z09（假突破降级 vs 真离开的互斥判定）：候选离开段只有同时满足“自身幅度不超过`ZS_FALSEBREAK_MAX_SPAN=1.5`倍区宽（`zg-zd`）”“被下一段方向性证伪（回到中枢内，或反向击穿另一侧）”“自身与中枢至少有0.1的重叠”三个条件，才降级为中枢成员、继续延伸中枢；否则一律确认为真离开。目的是避免“能被否定的离开段”与“能被吸收的大口袋段”定义重合，导致离开段必然被确认、三买/三卖退化为永真命题。

E-Z10（种子重叠门槛）：三笔/三段种子除满足O-Z02的区间求交外，还要求三者各自与`[zd,zg]`的重叠比例不低于`ZS_SEED_MIN_OVERLAP=0.15`（比成员门槛0.3更宽松），防止字面满足三段重叠、实际方向失真的组合被当成中枢起点。

E-Z11（v0.5新增：离开段须与中枢有接触）：v0.4的候选离开段判定里，`!overlaps`（候选对象与`[zd,zg]`毫无交集）被当作离开信号的一个独立触发条件，但`!overlaps`实际混着两种不同情形：(a)这一段真的把价格带出了中枢——自身与区间原本仍有接触，只是端点冲出去了；(b)这一段开始之前价格早就已经在区间外了，它只是在区间外面晃，自身与区间从未有过接触。只有(a)才配当"离开段"；把(b)也当离开段，会让离开段的起讫、进而`leaveDir`、三买/三卖的回踩参照点全部算错。v0.5的处理：候选离开对象`o`若自身`overlaps===true`（与`[zd,zg]`有接触）则直接采信为离开段；若`overlaps===false`，则从该对象前一个开始向前回溯，找第一个同时满足"自身与`[zd,zg]`有接触"和"终点落在区间外"的对象作为离开段，回溯下界为种子三笔的第三笔（即`i+2`）——也就是说，允许种子三笔的第三笔同时兼任离开段：中枢的框仍由三笔定义（成员数不变），但第三笔可以同时是那根真正带价格离开中枢的段。若回溯到下界仍找不到合格对象，则该中枢暂不确认离开（`leaveIndex=null`，`leaveObj=null`），循环按`endMember+1`推进，不强行指定一个离开段。已知连锁后果：既然离开段可能就是种子三笔的第三笔，而构造下一个中枢时又以`i=leaveIndex`起算，这根笔就会同时成为下一个中枢种子窗口的首笔——修正一个中枢的离开归属，会使其后中枢的种子窗口整体前移一笔，边界（`zd`/`zg`/起止日期/成员数）可能因此与修正前不同。这是本规则的必然推论，不是新缺陷，但意味着升级到v0.5后不能假设未直接涉及的历史中枢的边界保持逐字不变。

E-Z12（v0.5新增：离开方向按端点所处一侧判定）：`center.leaveDir`（价格离开到了中枢哪一侧）与`leaveObj.dir`（离开对象自身的涨跌斜率）是两个不同概念，v0.4混用为同一个字段，会把"整根笔/段都在中枢一侧、但自身走势方向朝另一边"的情形判反（例如一根整体处于中枢上方、自身向下走的笔，被判成"向下离开中枢"）。v0.5起`leaveDir`独立计算：取离开对象终点价，`>zg`记`leaveDir='up'`，`<zd`记`leaveDir='down'`；终点价落在`[zd,zg]`区间内（说明这个"离开段"其实并未真正离开）时不编造方向，`leaveDir=null`并在该中枢的`rejection_reasons`追加`leave_end_inside_range`。`leaveObj.dir`本身不受影响，仍表示对象自身斜率，两者允许不相等。R5的走势方向、R6背驰的`priceExtends`与产出方向、R7—R9买卖点的方向判定（1B/1S的买卖方向、2B/2S的延续方向比对、3B/3S的突破/回踩方向）一律改用`leaveDir`；只有R12力度模块里"两个推动段是否同向"的比较（`A.dir===C.dir`）仍用对象自身斜率，因为那里问的是推动段之间是否同向，与"离开到了哪一侧"是不同问题。`leaveDir`为`null`时，相关信号与背驰一律不生成，不回退到`leaveObj.dir`。

P-Z07（走势判定与O-Z05冲突，未解决）：`computeTrendSegments` 目前只用相邻两个中枢的核心区间比较——`cur.zd > prev.zg` 判上涨、`cur.zg < prev.zd` 判下跌，否则`range`；首个中枢固定`trend='pending'`。这与O-Z05“趋势分离检查外围波动关系，不能只比较ZD/ZG两个框”的文字直接冲突：当前引擎恰恰只比较了ZD/ZG两个框，未使用GG/DD及外围波动关系，也未做“同级中枢合法方向分离”的独立校验。在补上GG/DD比较逻辑并给出对应验证案例前，不得在文案中宣称走势类型已按第18/20课/O-Z05实现；产品侧展示走势结论时应注明这是保守简化判定，而非最终严格版本。

P-Z08（`leaveDir`随新K线到来发生方向修正，未解决）：对641根数据从as_of=130到as_of=640逐前缀重算（511个前缀），发现4处同一中枢（按`zd`/`zg`/起始日期匹配）的`leaveDir`随后续K线到来从`up`修正为`down`或反之。每一次修正在"当时可见"框架下都是合法的——更晚的K线揭示了此前判定所依据的离开段其实并未真正越界，或此前未被采信的对象才是真正的离开段——但这意味着同一个中枢在不同`as_of`下可能给出不一致的方向标注。这与第13节"真实前缀回放：每根输入的快照，与只给同一历史前缀的全量计算一致"的要求不冲突（因为每个前缀内部自洽），但暴露了一个尚未覆盖的验收缺口：现有验收未专门检查"同一结构对象的标注随时间推移是否会翻转，翻转是否都能定位到具体的新增证据"。在补上这类专项前缀回放测试并给出翻转原因的可追溯记录前，不得宣称`leaveDir`标注在事后修正意义上是稳定的。

## 10. 背驰：结构筛选与力度代理

O-DV01：依据第37课先检查趋势和比较角色、级别关系、相应新高/新低、离开段条件。盘整背驰与趋势背驰独立输出。不能把所有比较段硬规定为同一结构级别，也不能不加筛选混比。

E-DV02：第一版可选 MACD 面积代理：原始收盘序列算 EMA12/EMA26，DIF=差值，DEA=EMA9(DIF)，HIST=2*(DIF-DEA)。EMA 以首值初始化并记录暖启动长度；合并 K 线不用于重新计算 MACD。

`up_area=sum(max(HIST,0))`，`down_area=sum(max(-HIST,0))`。区间采用 `(start,end]` 防止共享端点重复计数；前段面积为0返回不可比较，不能除零或输出无限背驰。

E-DV03：比值小于1只表示该代理力度减弱；进行中后段尤其不能因面积暂小确认背驰。MACD零轴回拉等过滤条件若采用，必须单独定义量纲与阈值，不填“接近0”。

P-DV04：背驰结束证据及次级别定位待与递归完成算法联调。在此之前只能显示“结构条件检查＋MACD力度减弱候选”。价格斜率、幅度、成交量可列辅助证据，不以投票得分替代定义。

E-DV05（v0.4引擎现状：参照段选取与产出门槛）：`computeBeichi` 调用 `computeTrendStrength` 取得离开段C相对参照段A的MACD面积比，仅当满足以下全部条件才产出背驰候选/确认：（1）A、C方向一致——优先取本中枢的`enterObj`，若与C反向则回溯前面中枢中第一个与C同向的`leaveObj`（`basis='prev_leave_vs_leave'`），找不到同向参照段时状态为`incomparable`，不产出背驰；（2）A、C的MACD面积样本≥3根且>0；（3）A、C面积均不低于同级别全部对象MACD面积中位数的`STRENGTH_MIN_AREA_RATIO=0.5`倍，防止用近乎无动能的段做分母/分子；（4）比值`ratio=areaC/areaA < STRENGTH_WEAK(0.8)`；（5）离开段C的价格确实相对参照对象和中枢边界创出同向新极值（`priceExtends`）。五者都满足才登记为背驰标记，中枢未确认或离开段仍是最新未验证段时标`candidate`，否则`confirmed`。`STRENGTH_WEAK=0.8`/`STRENGTH_STRONG=1.2`是R12力度模块产出“力度减弱/持平/增强”文字标签的分档线，与背驰确认阈值共用同一个比值但不是两回事：力度标签允许比值落在`[0.8,1.2]`间报“持平”，背驰只在`<0.8`且价格创新极值时才成立，二者不能互相替代解读。

## 11. 买卖点和策略

| 对象 | 所需依赖 | 不足时处理 |
|---|---|---|
| B1/S1 | 对应趋势、背驰、末端完成证据 | 背驰候选，不标已确认一买/卖 |
| B2/S2 | 对应一买/卖、随后次级别上/下行及第一次回调/反弹完成 | 保留候选；前置点或回调条件失效则撤销 |
| B3/S3 | 具体中枢、次级别离开、首次次级别回试完成及边界条件 | 未完成回试不提前确认为三买/卖 |

E-BS01：本版固定保守工程政策：三买回试低点严格大于ZG、三卖回试高点严格小于ZD，才进入确认检查；相等输出 `boundary_touch`，不自动确认，越入区间撤销该严格候选。此政策比“不跌破/不升破”的文字解释更保守，不宣称原典排除相等。后续修改须升级规则版本。

E-BS02：二买回调低点严格高于前置一买锚点、二卖反弹高点严格低于前置一卖锚点才进入本版确认检查；相等单列边界态，穿越则撤销对应候选。价格条件仅为资格检查，不替代首次次级别回调/反弹和完成证据。此为保守工程选择，不声称是完整原文定义。

同一点可兼具 B2/B3，采用标签数组；不能为了唯一标签删除有效关系。理论锚点、算法识别时间、策略触发时间各自保存。

E-BS03（v0.4引擎现状：背驰与三买/卖同枢互斥）：`deriveSignals` 中，若同一中枢已产出1B/1S背驰信号，该中枢非`failed`的3B/3S会被降级为`candidate`并附`conflictNote`说明“同一中枢的离开段已出现背驰信号，三买/三卖仅作观察，不与背驰并列采信”。这是引擎新增的工程政策：同一段离开走势不应同时以“确认背驰”和“确认三买/卖”两种确认姿态并列呈现给用户，防止界面同时给出两个方向不冲突但语义互斥的“已确认”结论。1B/1S的失效价（`boundary`）取离开段自身极值（跌破一买离开段低点/涨破一卖离开段高点），不是中枢边界——因为背驰点往往已经远离中枢，用中枢边界会让信号一出现就处于“已失效”区间。

S-01：收盘突破已确认小高点可以是单独的右侧策略。必须指定小高点的分型/笔来源、所用周期、收盘还是盘中、是否允许等价及缺口成交。该条件不重定义 B1/B2/B3。

S-02：失败线须关联具体假设，例如回调低点失守、中枢边界被触及；不能统一设为“防守点前面的低点”。防守区宽度、退出执行、滑点等待策略规格，不参与基础结构成败判定。

E-R11a（v0.4引擎现状：R11简化实现）：`support.js` 里的 `supportResistance` 只取最近两个笔中枢的ZG/ZD作为压力/支撑线；`failureLine` 只取“最近一个信号”的`boundary`字段作为防守位（取不到时退回该信号所属结构对象的低/高点）。这是S-01/S-02设想的“观察区/压力/触发/失败线”体系的一个精简子集：目前没有独立的观察区、收盘突破触发规则、滑点/退出规格字段。产品文案介绍“操作参考”时应说明这是当前的简化版本，不等同第11节完整策略层交付。

## 12. 图形与解释验收

- 原始 K 线和标记共用时间/价格坐标；水平价位 y 恒定，不能画斜。
- 箭头尖端贴真实来源价格，文字可避让；标签偏移不能改变实际锚点。
- 候选虚线、确认实线；数据不足与拒绝不画成确认答案。
- 点击标记显示规则编号、组成对象、数值比较、确认时刻与未满足条件。
- 初始只显示 K 线、当前选定结构层；逐层展开，避免一次叠满所有标记。
- 提供“当时可见”和“事后结构”两种视图。默认当时可见，事后视图明确显示确认延迟。
- 日线显示分钟结构时使用真实分钟数据映射；不能由日线 OHLC 虚构分钟路径。

## 13. 验收体系

配套 `缠论自动标记工具_基础验收样例.json` 是规格级合成案例，不是真实行情，也不是经过引擎验证的金标准。

验收分四层：

1. 单项：包含顺序、同价、方向、分型、独立根数、成笔价格资格。
2. 状态：候选替换、撤销、确认、相等端点、启动上下文不足。
3. 结构：67/71/77/78/81全部边界图的数值版本，含不成立的近似配对。
4. 真实前缀回放：每根输入的快照，与只给同一历史前缀的全量计算一致；不可用完整未来图当作过去答案。

必要测试：未收盘改价；新增包含；缺失数据；数据修订；复权变更；缩放不改结果；镜像价格使上下判断对称；所有锚点可追溯；相同版本相同输入产生相同结果。

“已确认不变”只在数据与规则版本相同、确认依赖确实冻结时测试；若失败，修正冻结政策或算法，不删历史事件掩盖。

盈利、命中率、信号延迟属于后续独立统计。复现准确不等于策略有收益。

## 14. 执行顺序与发布门槛

| 阶段 | 交付 | 完成判据 | 当前状态（v0.4） |
|---|---|---|---|
| A 规则首版 | 本 SPEC、基础案例、交接更新 | 来源、E/O/S/P区分清楚 | 完成，本次为v0.4对齐补写 |
| B 基础引擎 | 包含/分型纯函数、笔候选与事件回放 | 单项和状态用例通过；B05/B06完成 | 包含/分型/笔已有可运行纯函数实现（`chan-algo.js`），批量重算非事件回放（E-B07）；B05/B06仍未做端到端验证 |
| C 线段审计与引擎 | 特征序列双情况、特殊线段 | 原文更正案例转录、配对测试通过 | 完整窗口扫描算法已实现（E-X09）；71/78/81数值案例仍未转录，P-X08未解决 |
| D 真实 K 线查看器 | 数据元信息、图层与解释卡 | 真行情来源核验、锚点和回放一致 | 已接入真实数据（招商银行600036，2024-01-19~2026-09-11，腾讯qfq前复权）并有可交互网页图层与详情卡（`缠论标记工具.dc.html`）；第13节要求的真实前缀回放一致性测试尚未做 |
| E 高层结构 | 中枢/走势/背驰/买卖点 | 递归与完成规则固定、前缀验证通过 | 双级别中枢（E-Z07）、背驰（E-DV05）、三类买卖点（E-BS01—03）已有可运行实现；走势判定与O-Z05冲突未解决（P-Z07），前缀验证测试未做 |
| F 力度与联立 | R12/R13及解释视图 | 第18节新增验收通过；父层规则与子层完成证据可追溯 | R12仅实现了R6/R11所需的MACD面积比子集，第16节E-L01—L07全量指标未实现；R13完全未实现 |

后续 AI 从阶段 B 开始；独立纯计算模块和样例优先。线段例外未通过前，不发布线段“严格确认”。本阶段不需要用户在多个笔算法之间作技术选择；沿本规格执行并在证据支持时版本化修订。

## 15. 模块编号及依赖更新

| 模块 | 内容 | 规则入口 |
|---|---|---|
| R0 | 包含 | O-K01、E-K02、O-K03、O-K04、E-K05、E-K06 |
| R1 | 分型 | O-F01、E-F02—04 |
| R2 | 笔 | O-B01—03、E-B04、P-B05、E-B06、E-B07 |
| R3 | 线段 | O-X01—07、P-X08、E-X09 |
| R4 | 中枢 | O-Z01—05、E-Z03、P-Z06、E-Z07—E-Z10、E-Z11、E-Z12 |
| R5 | 走势 | O-T01、O-Z05、P-Z06、P-Z07、P-Z08（依赖`leaveDir`语义，见E-Z12；比较逻辑未变但输入边界随v0.5变化） |
| R6 | 背驰 | O-DV01、E-DV03、P-DV04、E-DV05（`priceExtends`与产出方向依赖`leaveDir`，见E-Z12） |
| R7 | 一买一卖 | 第11节、P-DV04（1B/1S买卖方向依赖`leaveDir`，见E-Z12） |
| R8 | 二买二卖 | 第11节、E-BS02、E-BS03（2B/2S延续方向比对依赖`leaveDir`，见E-Z12） |
| R9 | 三买三卖 | 第11节、E-BS01、E-BS03（3B/3S突破/回踩方向依赖`leaveDir`，见E-Z12） |
| R10 | MACD | E-DV02、第16节E-L02 |
| R11 | 策略 | S-01、S-02、E-R11a |
| R12 | 独立力度 | E-L01—07（第16节全量指标未实现；已实现子集见E-DV05） |
| R13 | 多级别联立 | E-M01—07、P-M08（未实现） |

E-G01：模块编号不是固定的一次性执行顺序。父级结构依赖子级组成，联立模块读取各层快照并产生观察任务，不反向修改底层分型/笔以匹配预期买点。依赖变更按对象ID传播，只重算受影响对象；首次可用时间沿依赖取最大值。

E-G02：递归构造先生成结构候选，完成证据单独登记；不能要求父层确认先依赖小买点、小买点又依赖同一父层已确认，形成循环证明。未解决的完成依赖输出pending。

## 16. R12 独立力度引擎

这些指标属于明确命名的工程辅助量，不等同于唯一的原典“力度”。第15课均线面积口径仅列参考，本版未实现该口径；MACD辅助依据第24课。新增来源：[第15课](https://blog.sina.com.cn/s/blog_486e105c010007j8.html)。

E-L01（对象与时间）：每次计算绑定对象ID、方向、结构层级、起止锚点及as_of。比较必须由结构层提供角色合格的对象，不由力度层任意挑段。记录completed/partial；比较跨周期的原始MACD面积禁止。未完成后段显示partial，不输出最终背驰。

E-L02（MACD口径）：沿用E-DV02；EMA递推系数2/(n+1)，首收盘初始化EMA12/26，首DIF初始化DEA。默认至少130根已收盘原始K线作为预热，预热不足显示 `warmup_insufficient` 并禁止据此确认背驰；130为工程门槛，不保证消除全部初始化误差。使用固定数据起点，缩放不重新初始化。柱面积对 `(start,end]` 内原始K线累计；颜色可配置，以正负号而非颜色决定方向。

E-L03（公式）：设方向d为+1或-1，结构锚点价Ps、Pe；指标使用的原始K线周期相同。

| 指标 | 公式 | 特殊处理 |
|---|---|---|
| 推进率 | d*(Pe-Ps)/Ps | Ps<=0不计算百分比，返回价差及原因 |
| 推进速度 | 推进率/交易分钟数 | 时长由交易日历统计，含休市的自然时间不可替代；<=0不可计算 |
| 上行MACD面积 | Σmax(HIST,0) | 原始K线、固定区间 |
| 下行MACD面积 | Σmax(-HIST,0) | 原始K线、固定区间 |
| MACD方向峰值 | 对应方向柱绝对值最大值 | 区间无该方向柱记0；无数据记null |
| 收盘路径效率 | abs(C最后-C最初)/Σabs(Ci-Ci-1) | 固定在对象时间范围内的收盘序列；不足2点或分母0为null；分子不混用影线锚点 |
| 上行后的回撤比 | max(0,Pe-后续最低)/abs(Pe-Ps) | 至as_of的指定首次反向对象范围；无后续数据为null；可大于1 |
| 下行后的反弹比 | max(0,后续最高-Pe)/abs(Pe-Ps) | 同上，分母0为null |

回撤观察开始时间是结构端点之后，其数值只在实际数据出现时可知。OHLC不能分辨端点当根内部先后，回撤统计默认从端点之后的下一根开始，并说明该限制；后续更细数据可用时需独立版本。

E-L04（比较）：同一指标输出前值、后值、差、比值。前值<=0时不生成常规比值，给null和原因。后/前<1、=1、>1分别仅代表该指标减少、不变、增加。金额/价格类指标用固定精度；指标浮点比较容差固定为 `1e-12*max(1,abs(a),abs(b))`，不是价格结构的相等容差。

E-L05（不综合投票）：不输出未经标定的强度分数、不设置“低于0.8即背驰”等阈值。推进更快而面积更小显示“指标分歧”，由背驰模块继续检查结构，不强行归为全面变弱。

E-L06（可见性）：每个指标保存 `object_id, metric, value, unit, window_start, window_end, available_at, partial, warmup_status, comparison_id, unavailable_reason`。后续回撤不能回填成此前已经知道的弱势证据。

E-L07（发布约束）：完成态只说明指标对象按既定规则完成，不证明指标有预测效果。不同股票、周期及价格尺度的指标不直接比较；跨品种强弱排序不在本版范围。

## 17. R13 多级别联立与区间套

原典参考：[第61课](https://blog.sina.com.cn/s/blog_486e105c01000b9n.html)、第37课。本节将概念映射为工程任务与状态，不声称这些状态名来自原文。

E-M01（两个维度）：多周期观察与结构递归联立分开。前者对比不同周期快照，后者保存真实父子组成。仅有日线与30分钟图同时看多，不能认定完成严格区间套。

E-M02（归属）：父子节点记录 `parent_id, child_ids, level, timeframe, role, start,end`。子对象必须属于待检查父段的实际组成或有明确分解映射，不能只靠时间重叠。若父子关系尚不明确，输出unmapped，不给目标确认。

E-M03（观察任务）：`task_id, parent_id, target_type, target_child_id, as_of, prerequisite_ids, evidence_ids, invalidation_rules, status`。target_type区分下跌末段结束、上涨末段结束、二类回调/反弹结束、三类首次回试结束；禁止通用“小级别买点”直接满足所有目标。

E-M04（状态）：

```text
WAITING：父层目标资格成立，但内部证据不足
LOCAL_SIGNAL：有相关小级别信号，但父层全部条件未满足
TARGET_SATISFIED：父层该目标全部条件及完成证据满足
INVALIDATED：该目标的明确前提失效
UNMAPPED / DATA_INSUFFICIENT：结构映射或数据不足
```

TARGET_SATISFIED不自动等于大级别已上涨；只说明这个具体任务满足，父级走势由R5另行判定。INVALIDATED也不自动等于大级别反向趋势成立。

E-M05（事件更新）：收到新K线或结构事件→更新相关子结构→重新核查父层前提→检查目标内部证据→输出状态和缺失条件。确认时间取全部必要证据实际可用时间的最大值。父层三买回试越入中枢时，即便子层出现买点，严格三买任务仍失效；原小级别信号可保留为局部信号，不删除历史事实。

E-M06（区间套）：从具备外层条件的背驰候选段向内部逐层定位，每一层有对应比较对象和规则依据。外层力度或结构条件被否定时撤销外层候选；内层独立信号仍可存在。不能要求任意转折都必须出现无限层背驰；数据到最小粒度停止并标记限制。父层尚未完成时先保留候选，不靠内层信号跳过父层其他要求。

E-M07（数据与画图）：统一as_of，仅使用当时已收盘的周期数据形成正式快照，未收盘大周期可预览但标candidate。细周期数据必须真实取得。主图突出父段范围，展开其子结构；卡片并列父层状态、当前目标、小级别证据、尚缺条件、失效原因和确认时刻。

P-M08：在P-Z06递归完成与P-DV04结束条件解决前，联立可做结构展示和条件检查，不可发布为已验证的严格区间套交易确认。小级别结构的独立成立与大级别趋势转折不得合并为一个布尔值。

## 18. v0.2 验收补充与追踪

下表为待实现验收要求，不是已经通过的测试。既有JSON的20个案例只覆盖基础单项，未覆盖本节。

| 编号 | 输入或事件 | 预期 |
|---|---|---|
| A-L01 | 上行100→110、10交易分钟 | 推进率0.1，速度0.01/交易分钟 |
| A-L02 | 同周期HIST为2,-1,3 | 上行面积5，下行面积1，上行峰值3 |
| A-L03 | 收盘100,102,101,103 | 效率3/5；不得换成高低锚点价 |
| A-L04 | 前MACD面积0 | 比值null，明确不可比较 |
| A-L05 | 后段未完成，面积较小 | partial，不确认背驰 |
| A-L06 | 100→110，随后最低95 | 回撤比1.5，不截断到1，不提前显示 |
| A-L07 | 速度增加、面积下降 | 指标分歧，不生成综合强弱分数 |
| A-L08 | 历史不足130根或换计算起点 | 预热不足/新版本，禁止暗中复用旧确认 |
| A-M01 | 大级别下降，小级别上涨 | 两种状态可共存，不把父层自动改上涨 |
| A-M02 | 三买回试越入中枢，小级别有买点 | 原三买目标失效，局部信号保留 |
| A-M03 | 内部转强但回试完成证据不足 | LOCAL_SIGNAL，非TARGET_SATISFIED |
| A-M04 | 外层背驰前提被否定，内层背驰存在 | 撤销外层候选，不删除内层事实 |
| A-M05 | 日线未收盘、分钟线已收盘 | 日线预览候选与分钟正式快照区分 |
| A-M06 | 子对象仅时间重叠但无归属 | UNMAPPED，不确认父层目标 |
| A-M07 | 时间较早的结构点，较晚的确认事件 | 不允许早于确认事件触发回放信号 |
| A-BS01 | 三买回试最低恰等ZG | boundary_touch，本版不自动确认 |
| A-BS02 | 二买回调最低恰等一买锚点 | boundary_touch，本版不自动确认 |

发布清单：表格与SPEC版本一致、R0—R13映射完整、工程政策明确；P项未解决的模块保持实验/候选；全部端到端前缀测试和真实数据锚点测试完成后才能宣称相应引擎已验证。
