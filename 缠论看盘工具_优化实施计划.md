# 缠论看盘工具 · 优化实施计划

版本：2026-08 （一期）
配套：`缠论看盘工具.html`、`缠论学习路径与做T操作框架.md`、`看盘Web应用_产品判断框架.md`、`data/stocks.json`

---

## 一期目标（本次）

打通"数据这一公里"，把工具从"只能手动传 CSV"升级为"A股代码输入→自动拉取真实行情→切级别→跑缠论链路"，为后续二期（图上估值联动）、三期（口径校准）打地基。

## 关键技术结论（已实测验证）

| 项 | 结论 |
|---|---|
| **数据源选择** | 新浪 JSONP 接口稳定，用 `<script>` 标签载入可**绕开 CORS**，file:// 直接打开即可用。 |
| **东财接口** | 有 CORS（浏览器 fetch 绕不过）、响应时灵时不灵（反爬限流），浏览器端**不可靠**，二期不采用。 |
| **新浪覆盖范围** | **只覆盖 A股**（sh600036 / sh600519）。港股胜宏科技（02476.HK）返回 null，**不能自动拉取**。 |
| **各级别参数** | 日线 `scale=240`、30分钟 `scale=30`、5分钟 `scale=5`；日线 `datalen` 已验证到 300 根。 |
| **CORS 方案** | JSONP（`<script>` 标签）绕开跨域限制，无需本地服务器、无需部署。 |

## 一期任务清单

### 1. 真实行情源接入（新浪 JSONP）
- 输入框支持"名称 + 市场代码"。内置快捷映射：`招商银行`→`sh600036`、`贵州茅台`→`sh600519`。
- 点"拉取行情"或选股票时，用 `<script>` 标签按级别（日线/30分/5分）载入新浪数据，转成标准 `{date,open,high,low,close,volume}`。
- **A股**走自动拉取；**港股**检测到 `hk`/`02476` 类代码时明确提示"港股暂不支持自动拉取，请手动导入 CSV"，并回落手动 CSV。

### 2. 级别真实接入与口径分离
- 当前"级别按钮"只是换标签文本——改为**真实切换数据粒度**：点"日线/30分/5分"分别从新浪 `scale=240/30/5` 拉对应级别的真实K线。
- 画布顶部标注"当前级别"口径；没有数据的级别按钮置灰或提示先拉取。

### 3. CSV 解析健壮化
- 支持表头别名（date/日期/day、open/开盘、high/最高、low/最低、close/收盘、volume/vol/成交量）。
- 支持 `,` 和 `\t` 和连续空格分隔、`\r\n`/`\n` 换行、跳过无数据行。
- 保留手动 CSV 兜底路径（尤其港股）。

### 4. 失败处理与提示
- 无数据/网络失败时，界面给出明确提示并回落到上一次有数据的展示，不白屏。

## 后续期规划（不在本次范围）

- **二期**：图上价格标注（买卖点参考线、中枢上下沿数值、当前价横线）+ 估值区间入图（自动读 `data/stocks.json`）+ 估值约束自动填充。
- **三期**：缠论口径校准（分型放宽为单边极值、线段标注为近似口径、多中枢延伸防误判、二买二卖实时/复盘模式区分）。
- **四期**：复盘备注持久化（localStorage）、信号表增强、主图缩放+十字光标+悬浮读值、与 SQLite `chan_signals` 打通。

---

## 实施约束
- 直接在 `缠论看盘工具.html` 上改（不改文件名），每完成一个可验证的小阶段做一次 git commit。
- 保持单文件、无外部依赖、file:// 直接打开可用。
- 不破坏已有的三层结构、失效条件、估值纪律红线。
