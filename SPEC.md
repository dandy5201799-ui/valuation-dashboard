# 投资研究体系 SPEC

版本：2026-08-04（前端估值仪表盘与本地数据仓库版）  
用途：供 ChatGPT、Codex、Claude 等 AI 工具快速理解本项目的目标、方法、完成度与后续工作。  
项目根目录：`投资研究体系_定稿_2026-07-31`

## 0. 当前项目状态（2026-08-04）

本项目已从“Excel/Word 工作簿体系”收口为“手机端估值仪表盘 + SQLite 数据仓库”。

**部署（2026-08-04 新增）**：本目录已作为独立 Git 仓库同步至 GitHub 公开仓库 `dandy5201799-ui/valuation-dashboard`，并通过 GitHub Pages 发布，手机可直接访问：`https://dandy5201799-ui.github.io/valuation-dashboard/`（`index.html` 重定向到 `估值仪表盘.html`）。因免费版 GitHub Pages 仅支持公开仓库，该链接对任何持有者可见（不含个人身份信息，但含真实估值结论与仓位建议），已与用户确认此取舍。GitHub Pages 只读取仓库内容，本地跑完数据更新后必须提交并推送，线上页面才会刷新；当前 Codex 定时任务已改为“数据更新 → JSON/HTML校验 → 有变化才 `git add -A && git commit && git push`”，非交易日或无变化时不提交不推送。`.gitignore` 已排除 `.claude/settings.local.json`、`.DS_Store`、`dashboard.html`（本地模拟器测试用的临时符号链接）。

当前正式运行链路：

```text
行情与市场估值数据
        ↓
SQLite：05_数据仓库/investment_research.sqlite
        ↓
JSON：data/stocks.json、data/market_temperature.json
        ↓
前端：估值仪表盘.html
```

日常更新只保留三个脚本：

| 脚本 | 职责 |
|---|---|
| `../update_stock_valuations.py` | 更新三只股票行情，写入SQLite，重建个股前端数据 |
| `../update_market_temperature.py` | 更新三大市场估值温度，写入SQLite，重建市场前端数据 |
| `../build_mobile_dashboard.py` | 从SQLite生成前端JSON，并回写HTML fallback |

Excel工作簿、Word研究卡、WPS同步脚本、旧版工作簿生成脚本已经转为归档资料，不再作为日常数据源，也不再同步WPS云文档。

## 1. 项目目标

建立一套可重复、可更新、能实际约束交易的长期投资研究体系，而不是收集零散公司资料。

统一决策链：

> 市场估值决定投入多少 → 产业地图决定研究哪里 → 公司质量决定是否值得长期跟踪 → 估值决定当前价格是否值得买 → 监控链验证假设 → 仓位规则形成买入、持有或减仓动作

核心原则：

- 不以短期股价预测为中心，以未来五年总股东回报和永久损失风险为中心。
- 不把“好公司”等同于“任何价格都可以买”。
- 不直接跟随机构目标价；重点分析当前价格隐含的长期增长、ROE、利润率和风险假设。
- 消息本身不触发交易。先判断它是否改变长期假设，再更新估值和仓位。
- 所有估值使用悲观、中性、乐观三情景；中性决定主体区间，悲观检验下行，乐观只作上限参考。

## 2. 学习与研究路径

1. **市场与仓位**：理解PE/PB、股债相对吸引力、风险溢价和危机状态；先确定目标市场适合投入几成。
2. **产业地图**：理解需求、产业环节、利润池、竞争格局和上下游传导链。
3. **公司横向比较**：比较同一环节公司的产业位置、护城河、利润率、ROIC、周期性和估值。
4. **公司研究**：研究商业模式、增长来源、资本配置、核心指标和逻辑失效条件。
5. **估值与隐含假设**：以当前价格倒推所需经营结果，再与基本面判断比较。
6. **监控与复盘**：按日、月、季、年分层更新，并记录假设变化，而不是只记录股价涨跌。

## 3. 正式文件及定位

| 文件 | 定位 | 当前状态 |
|---|---|---|
| `估值仪表盘.html` | 手机端估值页面主文件 | 正式前端入口 |
| `data/stocks.json` | 贵州茅台、招商银行A、胜宏科技H估值展示数据 | 由SQLite生成 |
| `data/market_temperature.json` | A股、港股、美股市场估值温度 | 由市场取数脚本生成 |
| `data/market_temperature.meta.json` | 市场温度的数据来源、新鲜度和漂移告警 | 由市场取数脚本生成 |
| `05_数据仓库/investment_research.sqlite` | 本地原始数据、估值输入、试算结果和更新日志数据库 | 当前唯一机器数据底座 |
| `05_数据仓库/schema.sql` | 本地数据库结构定义 | 需维护 |
| `05_数据仓库/README.md` | 表清单、季度更新标准SQL流程、常用查询 | 需维护 |
| `SPEC.md` | 模型、数据、更新规则说明 | 需维护 |

已归档资料：原Excel工作簿、Word研究卡、旧版HTML产业图、历史输出、预览图、WPS同步脚本、Excel/Word生成脚本。它们可作历史参考，但不得再作为日常前端数据源。

形式分工：HTML负责人读展示；JSON负责前端数据；SQLite负责保存可追溯的原始数据和每次模型试算记录；归档工作簿/Word只作历史底稿。

## 4. 已采用的估值框架

### 价格区间模型与数据沉淀规则（2026-08-03 起）

本项目同时保留两套价格区间，二者用途不同，不能互相替代：

| 区间 | 回答的问题 | 用途 | 更新频率 |
|---|---|---|---|
| 实用合理价格区间 | 当前价格在日常投资语境下大致贵不贵 | 首页、看盘、初步行动判断 | 财报后复核，必要时月度确认 |
| 严格模型价格区间 | 若要求某个年化回报率，今天最多应付多少钱 | 估值逻辑校验、隐含假设检查 | 季度财报后试算，年度复核结构参数 |

#### 实用合理价格区间

实用区间服务于日常阅读和决策，不追求单点精确，而追求稳定、可解释、方便复核。

基本形式：

> 实用合理价格区间 = 可持续盈利或净资产基数 × 实用估值倍数区间

不同公司取不同基数：

| 标的类型 | 基数 | 倍数 | 示例口径 |
|---|---|---|---|
| 消费公司 | 可持续EPS | 实用PE区间 | 贵州茅台：EPS × PE |
| 银行 | BVPS或一年后BVPS | 实用PB区间 | 招商银行：BVPS × PB |
| 成长制造股 | 选定利润口径对应EPS | 实用PE区间 | 胜宏科技H：2026E或升级后中周期利润 × PE |

实用区间的参数可以来自市场常用倍数、历史估值区间、同业比较和严格模型结果的交叉验证，但必须写清楚**基数是什么、倍数为什么合理、置信度高低**。它允许用“18–22倍PE”“0.95–1.15倍PB”这类便于人理解的区间，但不得伪装成客观精确值。

#### 严格模型价格区间

严格区间是预期回报模型。它的核心问题是：

> 假设未来N年的经营路径，如果我要求某个年化回报率，今天最多应该付多少钱？

严格区间必须**正向推导**：先定义经营情景与目标回报带，再按模型计算价格。不得为了让结果看起来顺眼而倒推参数。严格区间只纳入“中性经营假设 + 较高目标回报要求”的买入价带；低要求回报、乐观情景、券商目标价或上行参考**不得**进入严格区间。

统一框架：

> 严格价格 = 未来现金流或超额收益的折现值 + 终值折现值

也可以反过来使用：

> 当前价格 + 未来经营路径 → 反推出预期年化回报

三只股票的严格模型不同：

| 标的 | 模型 | 起点 | 路径 | 终值 | 严格区间形成方式 |
|---|---|---|---|---|---|
| 贵州茅台 | 两阶段FCFE＋退出定价 | EPS | 未来5年利润增长、ROE、FCFE | 第5年EPS × 终局PE | 用12%、10%目标回报率折现得到严格买入价带；8%只作乐观参考 |
| 招商银行A | 两阶段剩余收益法 | BVPS | 未来ROE路径、BVPS滚动、分红 | 稳定期PB=(ROE−g)/(k−g) | 用14%、11.5%要求回报率计算严格买入价带；9%只作乐观参考 |
| 胜宏科技H | 五年利润路径＋退出倍数/H模型＋反向指标 | 多口径利润 | 2026E、升级后中周期、历史中周期等利润路径 | 退出PE或H模型终局PE | 不用单一严格区间作结论，必须并列多口径和置信度 |

严格模型的输入分四类：

| 输入类型 | 作用 | 来源属性 | 入库要求 |
|---|---|---|---|
| 盈利/BVPS/利润基数 | 决定估值起点 | 财报事实 | 写入`raw_observations`，并在`valuation_inputs`标为`fact` |
| 未来增长路径 | 决定未来现金流或ROE路径 | 人工假设 | 写入`valuation_inputs`，标为`assumption` |
| 终局PE/PB/ROE | 决定终值，通常最敏感 | 推导＋人工判断 | 写入`valuation_inputs`，标为`derived`或`assumption`并附公式说明 |
| 要求回报率k | 决定折现强度 | 人工设定 | 写入`valuation_inputs`，标为`assumption` |

#### 季度更新流程

每个季度财报出来后，按以下顺序更新，不得先改结论再找参数：

1. 记录财报原始数据：收入、利润、EPS、BVPS、ROE、毛利率、净利率、经营现金流、分红、关键经营指标。
2. 与上一期、本模型基线、历史趋势比较，判断是数据波动、经营阶段变化，还是长期逻辑变化。
3. 先更新盈利/BVPS基数和未来五年路径。
4. 只有当连续证据显示长期ROE、利润率、增长中枢、风险溢价或资本回报发生结构变化时，才更新终局PE/PB/ROE和要求回报率。
5. 重新计算实用合理区间、严格模型区间、预期年化回报和当前价格位置。
6. 把原始数据、关键输入、试算结果、结论变化和原因写入数据库。
7. 再运行 `../build_mobile_dashboard.py` 更新前端JSON与人读页面。

区分三种更新：

| 更新类型 | 典型情况 | 是否改估值区间 |
|---|---|---|
| Data-only | 价格或单季数据变化，但长期假设不变 | 只改当前位置，不改区间 |
| Path update | EPS/BVPS/利润路径变化，但终局倍数不变 | 改区间，但不改长期倍数 |
| Structural update | 长期ROE、增长中枢、利润率、风险或商业逻辑变化 | 改区间，也可能改终局倍数或模型 |

#### 本地数据库

本地数据库路径：

> `05_数据仓库/investment_research.sqlite`

结构定义：

> `05_数据仓库/schema.sql`

重建脚本（删旧库、按`schema.sql`重建、全量装载已核验数据、跑验证）：

> `05_数据仓库/rebuild_db.py`

使用与查询说明：

> `05_数据仓库/README.md`

数据库只解决一件事：让每次取数、每次估值输入、每次试算结果都能被追溯和复用。前端页面展示最新结论，但不得覆盖掉历史试算记录。

核心表（结构版本 2026-08-03）：

| 表 | 保存什么 | 本轮新增/变更 |
|---|---|---|
| `securities` | 股票、市场、币种、商业类型；含仅作peer的同业 | 新增`002463.SZ`沪电股份（胜宏净利率同业锚） |
| `data_sources` | 数据来源、类型、可靠性说明 | 新增`frequency`、`is_official`、`coverage_start`、`coverage_end`、`cross_validated_note` |
| `raw_observations` | 财报、行业、指数、经营指标等原始观测值 | 新增唯一键`(security_id, metric_code, period_end, frequency)`；新增`fetched_at`、`is_restated`；`value`改为可空以保留"接口异常已剔除"的占位行 |
| `price_snapshots` | 每日价格、市值、PE、PB、股息率 | — |
| `model_versions` | 模型名称、版本、说明和对应SPEC章节 | 新增`status`（`active`/`deprecated`）、`deprecated_reason`、`superseded_by_id` |
| `valuation_runs` | 每次估值运行的日期、模型、价格、区间、结论和置信度 | `confidence`拆为**必填**的`confidence_earnings`与`confidence_multiple`（值域 高/中高/中/低）；原`confidence`降级为可空的综合值，不得单独引用 |
| `valuation_inputs` | 每次估值使用的关键参数及其fact/assumption/derived属性 | — |
| **`profit_basis_tiers`** | **多口径利润分层：口径名、金额、单位、证据等级A/B/C、推导过程、隐含倍数** | **本轮新增。周期股或产品结构升级中的公司无法用单一"正常化利润"表达时必须使用（胜宏科技H四层口径）** |
| `scenario_results` | 悲观、中性、乐观或不同要求回报率下的估值结果 | — |
| `assumption_change_log` | 参数变化、原因、证据和关联估值运行 | `security_id`改为可空并新增`market_code`，以容纳市场级（沪深300/恒生/标普500）口径变更 |
| `market_position_runs` | 市场估值与仓位模型的历史运行结果 | 新增`eps_basis`（`current_ttm`/`normalized_pb_roe`/`third_party_normalized`）、`pb_current`、`pb_median`、`pe_median`、`long_run_roe`、`calibrated`、`terminal_pe`；新增唯一键`(market_code, valuation_date)` |
| `crisis_metric_observations` | VIX、HIBOR利差等危机指标历史观测 | 新增唯一键`(market_code, metric_code, observation_date)` |

视图：

| 视图 | 用途 |
|---|---|
| `v_current_positions` | 每只证券**最新一次**`valuation_run`的 ticker/name/price/实用区间/严格区间/两维置信度/结论。日常看盘和仪表盘只读这张 |
| `latest_valuation_runs` | 最新估值日的完整run明细（兼容旧查询） |

最低入库标准：

- 日更行情：至少写入`price_snapshots`；若同步刷新当前估值位置，可新增`valuation_runs`的`run_type='price_only'`。
- 季度财报：必须写入`raw_observations`、`valuation_runs`、`valuation_inputs`、`scenario_results`；周期股另加`profit_basis_tiers`；若假设变化，必须写入`assumption_change_log`。
- 模型修订：必须新增`model_versions`或更改版本号，不得在旧版本下覆盖历史结果。
- 市场与仓位复核：必须写入`market_position_runs`，`eps_basis`必填，并用`calibrated`标注阈值是否已校准。
- 置信度：`confidence_earnings`与`confidence_multiple`均必填。盈利口径与倍数口径的把握程度不同时**不得合并成一个数**。

#### 价格区间概率与市场观点（2026-08-04 新增）

个股详情页新增两个模块，数据来源与方法：

| 模块 | 方法 | 真实输入 | 存储位置 |
|---|---|---|---|
| 价格区间概率 | 对数正态分布，中位数=现价（无方向漂移假设），标准差=已实现年化波动率σ×√T；触达概率用driftless布朗运动反射原理公式 | σ来自东方财富历史日线（茅台/招行各1,109个交易日，2022-01-04–2026-08-03）；胜宏H股仅71个交易日，改用同一实体A股300476.SZ的4.6年历史作参考并明确标注 | `security_narratives.field_key='vol'`（JSON） |
| 市场观点 | 东方财富研报库真实卖方研报：评级分布、EPS/PE一致预期、公开目标价（如有） | 茅台26份研报(2026-04起)+7份公开目标价；招行5份研报；胜宏H股无覆盖，引用同实体A股300476.SZ的4份研报 | `security_narratives.field_key='market'`（JSON） |

两个模块都不是"预测"：价格区间概率明确假设无方向漂移，只描述历史波动率隐含的分散度；市场观点只做"对比假设"，不作为跟随目标价的依据（见本节开头核心原则）。

**市场观点·估值倍数对比框（2026-08-04 补充）**：`market.mktMultiple` / `market.ourMultiple` / `market.stance` 三个字段，仅在能从真实数据推导时才写入，不得填入设计稿的示例占位值（如 `¥49–58`、`PB 1.1–1.3×`，这些是 `valuation-v2-market.dc.html` 的demo数据，非真实招行数据）。已实现：茅台 `mktMultiple`="PE 19–21×"（来自研报`diffNote`中已核实的口径）、`ourMultiple`="PE 18–22×"（=`fair`区间÷2025 EPS，18.00–22.00精确对应）、`stance`="基本一致"；招行 `mktMultiple`="PB ≈0.89×"（=`impliedPrice`39÷BVPS 43.90）、`ourMultiple`="PB 0.95–1.15×"（=`fair`区间÷BVPS）、`stance`="更保守"。胜宏无真实市场倍数数据（H股无独立覆盖，A股参照研报未披露估值倍数假设），`market`对象不含这三个字段，前端`marketBlock()`按`m.mktMultiple`是否存在判断是否渲染对比框，缺失时不渲染、不编造。

**前端布局精简与还原度修正（2026-08-04）**：

- `topBar()` 改为条件渲染：数据正常时不占空间（去掉常规态"估值仪表盘"应用名与"数据截至 XXXX"，与"我的股票"旁已有的收盘日期重复）；仅在 `state.stale` 为真（抓取失败、页面回退到内嵌快照）时渲染警示条——这条不可删除，删除即违反"不得静默把旧数据当最新"的硬性要求。
- 列表页 `viewList()` 去掉两行说明文字："市场温度只回答…"（`d.boundaryNote`，市场详情页 `viewMarket()` 底部仍保留，那里是深一层的方法论说明）、"状态＝严格模型口径；偏离中枢＝相对实用区间中枢"。
- 个股详情页 `viewDetail()` 的"①"区块按设计稿重做：标题由"估值结论"改为"结论与行动"，结论段落前新增"当前价格"（大字号）/"偏离区间中枢"（`d.devStr`，同列表页复用同一份 `dev`/`devFg` 计算）两栏展示，随后再接原有结论文字与色带。
- 修复茅台 `verdict` 文案中的"18–22× **× 2025** EPS"重复乘号（源头在 `security_narratives.verdict`，已在 SQLite 更正并重跑 `build_mobile_dashboard.py`）。

**已知待复核项（未处理，非本次改动）**：`market_temperature_runs`/`FALLBACK_MARKET.warnings` 中有一条"A股.pb_pct 抓取值48.2000相对核验快照52.8400偏离8.8%，结论文案需复核"的数据质量警告，目前只存在于JSON数据里，前端未渲染、也未处理。价格区间概率模块五档百分比为独立四舍五入，个别情况下加总不等于100%（数值本身无误，只是显示上偶尔凑不满整数）。

废弃口径（已废弃，不得再使用）：

> 下列做法自 2026-08-03 起全部作废。数据库中不删除历史记录，而是把对应模型置为 `model_versions.status = 'deprecated'`，填写 `deprecated_reason` 并用 `superseded_by_id` 指向替代模型。查询已废弃口径：
> `select model_code, version_label, deprecated_reason, superseded_by_id from model_versions where status = 'deprecated';`

| 已废弃做法 | 状态 | 替代机制 | `model_versions` 记录 |
|---|---|---|---|
| ~~单季利润增速直接替换长期增长率~~ | 已废弃 | 单季只校准前1—3年路径，长期增长中枢需结构性证据 | —（规则级，见"季度校准规则"） |
| ~~把"当期TTM EPS"称为"正常化EPS"~~ | 已废弃 | PB—ROE正常化机制（`eps_basis='normalized_pb_roe'`） | `market_current_ttm_as_normalized` v0，`status='deprecated'` |
| ~~胜宏科技H用单一严格区间或单一估值标签作结论~~ | 已废弃 | `profit_basis_tiers`四层利润口径＋两维置信度，并列呈现、不宣称精确合理价值 | —（结构级，见 `profit_basis_tiers`） |
| ~~用压力测试值作为买入或"明显低估"行动阈值~~ | 已废弃 | 行动阈值只取中性情景在不同要求回报下的价值；压力值只作下行参考线（招行阈值 29.25 → 33.39） | —（规则级，见"跨模型统一规则"第2条） |
| ~~手填终局PE/PB但不给公式、同业锚或历史锚~~ | 已废弃 | 终局倍数必须为`valuation_inputs.input_type='derived'`且在`formula_note`写出公式，并列2—4个锚显示分散度（胜宏终局PE 22 → 12.4） | `shenghong_manual_terminal_pe` v0，`status='deprecated'` |

### 跨模型统一规则（2026-08-02 起）

这三条适用于所有个股与市场模型，优先级高于各自的局部约定：

1. **终局倍数必须推导，不得手填，且必须给出参照区间而非单值。** 终局PE/PB由稳定期的增长率、ROE、派息率和要求回报推导；可比公司倍数只能作参照或上限，且必须有来源。若第5年增速仍明显高于稳定期增速，用H模型（淡出）而非直接套永续。但**任何单一公式都不是唯一正确答案**——H模型假设增速线性衰减、派息率恒定、不约束再投资一致性，对强周期重资本企业只能作参照。应同时列出2—4个锚并显示其分散度。
2. **区分三种情景，用途与折现率规则不同。**

   | 类型 | 基本面 | 折现率 | 可否用作行动阈值 |
   |---|---|---|---|
   | 中性估值情景 | 中枢 | 要求回报k | 是 |
   | 经营悲观情景 | 下调 | **与中性同k** | 是 |
   | 危机压力测试 | 下调 | **可同时上调k** | **否**，只作下行参考线 |

   基本面恶化与风险溢价上升在2008/2020式危机中确实同时发生，因此压力测试中二者共振是**合理的**，不是简单的重复计价。但其输出不是概率中性的合理价值。**自2026-08-02起，压力测试值不得用作"明显低估"等行动阈值**（招行原用悲观值做门槛，已改）。
3. **"正常化"是有定义的词，不能当形容词用。** 只有经过跨周期平滑（长期平均ROE×净资产、中周期ROIC×投入资本、中周期利润率×正常化收入）的数字才能叫正常化盈利。单年实际值叫"当期/已实现"，未来单年预测叫"预测/情景基线"。**周期股必须同时呈现预测利润与正常化利润，二者相差数倍时不得用单一估值标签作结论。**
4. **反向估值指标须标注其依赖边界。** "所需退出倍数""同倍数退出IRR""隐含ROE/增长"这类反推指标只消除了对*某一个*假设的依赖，不等于稳健。必须在同格注明它仍依赖什么。
5. **仓位映射应连续，不用阶梯。** 阶梯映射会因输入微调产生跳变（实测：市场模型填入PB后A股仓位由30%跳至70%）。输入未校准时更应取区间下沿。

### 市场

- 先用正常化盈利和长期回报模型判断绝对估值及基础仓位。
- 再用股票相对债券的吸引力调节仓位。
- 最后用危机状态、极端指标和数据质量限制动作。
- 各市场分别视为100%可配置上限，不在本阶段规定A股、港股、美股之间的总资产配比。
- **市场估值温度（列表页展示模块，2026-08-03新增）**：用于在股票估值仪表盘首页展示A股、港股、美股三个市场的外部估值高低。它是“市场背景温度计”，不是个股估值模型，也不是最终仓位模型。主口径采用外部可复核的PE/PB/股息率/历史分位数据；内部正常化盈利模型只作解释和差异说明。

  **五级估值温度规则**

  | 历史分位 | 温度等级 | 页面含义 |
  |---|---|---|
  | 0%–20% | 明显低估 | 历史上很便宜，可提高关注 |
  | 20%–40% | 合理偏低 | 有配置吸引力 |
  | 40%–60% | 合理 | 中性，不贵不便宜 |
  | 60%–80% | 偏贵 | 不便宜，谨慎新增 |
  | 80%–100% | 明显高估 | 历史高位，控制仓位 |

  **列表页展示字段**

  | 字段 | 说明 |
  |---|---|
  | 市场名称 | A股 / 港股 / 美股 |
  | 代表指数 | 沪深300 / 恒生指数 / 标普500 |
  | 五级估值状态 | 明显低估、合理偏低、合理、偏贵、明显高估 |
  | 核心一句话 | 用一句话解释结论，如“PE偏高但PB中性”“PE/PB均处近10年高位” |
  | 点击入口 | 进入该市场的估值依据详情 |

  **详情页展示字段**

  | 字段 | 说明 |
  |---|---|
  | 外部数据源 | 中证指数官网、蛋卷/雪球估值、百分位网、乐咕/亿牛、Advisor Perspectives/GuruFocus/StreetStats等 |
  | 当前估值指标 | PE、PB、股息率；美股可补CAPE或Forward PE |
  | 历史分位 | PE分位、PB分位，必要时补PS分位 |
  | 五级判断依据 | 哪些指标落在哪个分位区间，因此判为哪一级 |
  | 与内部模型差异 | 若外部分位与内部正常化模型不一致，说明原因，不强行合并 |
  | 更新日期 | 数据截止日和页面更新时间 |

  **判断合成原则**

  1. 若PE与PB处于同一大区间，直接采用该等级。
  2. 若PE与PB分歧明显，优先展示“分歧型结论”，例如“A股：PE偏贵，PB合理，可能受盈利低谷影响”。
  3. 若PE、PB至少两个主指标均高于80%分位，显示“明显高估”。
  4. 若PE高而PB中性或偏低，不直接判为明显高估；页面应提示“盈利口径可能偏弱，需结合正常化模型解释”。
  5. 美股可用CAPE作为长周期温度锚，Forward PE作为短期盈利预期锚；二者冲突时同时展示，不只给一个结论。

  **当前试算口径（仅作为UI默认示例，后续由数据更新脚本覆盖）**

  | 市场 | 指数 | 外部数据口径 | 五级温度 | 说明 |
  |---|---|---|---|---|
  | A股 | 沪深300 | PE分位约86.68%，PB分位约52.84% | 偏贵 | PE高、PB中性，表面不便宜，但可能受盈利低谷影响 |
  | 港股 | 恒生指数 | PE近10年分位约80%，PB近10年分位约82% | 明显高估/偏贵上沿 | PE与PB均处高位，估值不便宜 |
  | 美股 | 标普500 | CAPE接近99分位，TTM PE约25–26倍，Forward PE约20倍 | 明显高估 | 长周期估值高位，但盈利增长预期仍支撑短期市场 |

  **与仓位模型的关系**

  市场估值温度只负责回答“市场表面贵不贵”。仓位模型仍按以下顺序输出：外部估值温度 → 内部正常化盈利解释 → 股票相对债券吸引力 → 危机状态限制 → 连续仓位映射。不得把列表页的五级温度直接等同于最终仓位。

- **EPS口径（2026-08-02 修订）**：美股使用DWS长期估值框架的正常化EPS；A股与港股原为当期TTM EPS（点位÷PE反算），并未正常化。现已在`01合理价值`加入基于ROE的正常化机制：

  > BVPS = 点位 ÷ PB；ROE = PB ÷ PE；正常化EPS = BVPS × 长期平均ROE

  该式绕开"需要指数历史EPS序列"的障碍，与招行的PB—ROE模型同构。只需填两个输入：当前PB、近10年PB中位数（长期平均ROE可直填，留空则按 PB中位数÷PE中位数 近似）。**2026-08-02 已完成校准。** 数据源：蛋卷(雪球基金) `djapi/index_eva/{pb,pe,roe}_history/{SH000300,HKHSI}`，515个观测点、2016-07-31至2026-07-30、**周频、第三方**，非中证/恒生官方日频序列。分位由我方从原始序列独立复算，与接口水平线交叉校验一致（恒指PB中位复算1.0938 vs 接口1.0965）。**官方交叉验证：中证指数官方沪深300 PE(2026-07-31)=14.36 vs 蛋卷14.3751，偏差0.10%，通过。**

  已填入：沪深300 PB 1.4655/PE 14.3751/PB中位1.4626/PE中位12.7283；恒生 PB 1.2319/PE 12.3595/PB中位1.0938/PE中位10.4401。终局PE已同步改用同源PE中位数（A股13.19→12.7283，港股9.51→10.4401），确保均值回归只计入一次。

  **结论变化：A股正常化EPS 319.2→359.8（系数1.127），合理点位4242→4639，当前/合理1.081→0.989，状态由"合理偏贵"转为"合理"，连续仓位34%→52%。港股正常化系数1.051，合理点位18499→20543，状态仍"明显高估"、仓位仍0%。美股不变。**

  *仍未验证*：中证与恒生官方的十年日频PB序列均未取得；接口ROE = PB÷PE 为派生值，不构成独立验证，且其序列仅自2019-11起（6.7年），不足以支撑十年正常化ROE——故长期平均ROE改由 PB中位÷PE中位 推导。

  *是否重复计入均值回归*：不会——前提是长期平均ROE由 PB中位数÷PE中位数 推导。此时终值 ∝ 当前BVPS×PB中位数，均值回归只通过 PB_now→PB_med 进入**一次**。但若改用外部来源的ROE直填而终局PE仍取PE中位数，二者不再自洽，会重复或抵消均值回归；表内已加"口径自洽校验"列自动提示。

- **跨市场比较的限制**：三市场EPS口径不一致时，各市场的**绝对**结论（自身价格 vs 自身合理价值）仍有效，但**跨市场相对性价比排序不成立**。本体系原本就不规定市场间资产配比，该限制不影响既有用法，但不得据此排序。
- **仓位映射**：自2026-08-02改为连续线性函数（比值0.8→90%、1.2→10%），取代原五档阶梯，消除输入微调导致的跳变；美股仍受40%上限约束。
- **危机状态**：改为**绝对阈值触发＋历史分位校验**双层结构。**2026-08-02 美股（VIX）与港股（隔夜－1个月HIBOR利差）已用2008/2015/2020/2022四次事件的HKMA/FRED官方历史数据完成绝对阈值校准**，详见`02危机状态`表内新增的"已校准绝对阈值"块。**关键发现：HIBOR/银行体系总结余的绝对水平不能作危机指标**——实测1个月HIBOR在2008危机峰值4.99%，而2022年非危机期峰值5.08%（更高）、2020流动性危机时仅2.25%；银行体系总结余2015年非危机期高达4263亿港元，2008年均值仅710亿。这些绝对水平反映的是美元利率周期与金管局干预状态，不是压力，故改用相对指标（VIX、隔夜/1个月HIBOR利差）。**A股仍未校准**（中债信用利差接口返回HTTP 404，未取得可靠序列），分档继续依赖人工判断。**在A股阈值校准完成前，A股危机层结论只作定性刹车，不用于精确调整仓位；美股/港股已可用于分档判断。**

### 贵州茅台

- **模型准确名称：两阶段FCFE＋退出定价的预期回报模型（非单一股权成本DCF）。** 终值按稳定期8%要求回报推导终局PE（约18.5倍），五年现金流按投资者10%目标回报折现。两个数不是同一个概念，改一个不会自动改另一个。此前称"标准两阶段FCFE"不准确，已更正。
- FCFE按 EPS×(1−g/ROE) 计算，派息率由留存率反推，不手填。
- **"合理价值1203元"的准确名称是"10%目标回报买入价"**，不是内在价值。已在表内更名。
- 经营拆解：真实需求 → 批价/库存/渠道利润 → 投放与提价 → 收入、利润和现金流。
- 当前中性研究基线以成熟期约5%的利润增长为中心，只能由季度和年度证据校准，不能由单季同比机械替换。
- **已知边界（表内已标注）**：
  - *净现金*：不能无条件断言低估。EPS已含现金利息收入，直接加回净现金会重复计算。以终局PE 18.54倍资本化利息收入，等价于把现金按 1/18.54＝**5.39%** 的税后收益率计价：低于此收益率则现金被低估，高于则被高估。茅台2025年末货币资金517亿元、无有息负债，实际税后现金收益率远低于5.39%，故方向上仍为低估，但**幅度需拆分税后净利息收入后才能量化，当前不给具体金额**。
  - *情景*：悲观情景同时下调增长/ROE并上调要求回报至9.5%，功能上接近压力测试。已核对：茅台五档区间由中性情景在12%/10%/8%三个目标回报下的价值构成，**不使用悲观值作阈值**，故不影响行动规则。

### 招商银行

- 模型：两阶段剩余收益法。RIₜ=(ROEₜ−kₑ)×期初BVPSₜ，终值用稳定期PB=(ROE−g)/(kₑ−g)。不套用普通消费股PE模型。
- 经营拆解：负债成本/存款结构 → 资产收益率与净息差 → 非息收入 → 信用成本和资产质量 → ROE、资本与分红。
- 估值必须使用三情景，并区分当期ROE与可持续ROE。
- **2026-08-02 行动规则修订（本轮唯一改变行动规则的改动）**：原"明显低估"门槛用的是**悲观情景今日价值（约29.25元）**，即压力测试值。这要求股价跌破危机情景价值才算低估，该门几乎永不触发，且混淆了"相对合理价值便宜"与"相对危机价值便宜"。已改为与茅台同构：五档由**中性情景在14%/11.5%/9%三个要求回报下的价值**构成；悲观值保留显示但改标为"压力情景参考线（不参与分档）"。
- **已知边界（表内已标注）**：① kₑ=11.5%是要求回报(hurdle)，不是CAPM股权成本，相对中国10年国债隐含近10个百分点风险溢价——"市场隐含稳定ROE"完全由kₑ决定（k=8%→7.5%、k=10%→9.3%、k=11.5%→10.7%、k=13%→12.0%），引用时必须同时报出kₑ与g；② EPS=期初BVPS×ROE，相对披露的加权ROE口径约低估2%，方向保守；③ 起始BVPS为2026Q1减已除权股息，估值日为7月末，未计入约一个季度留存利润，PB约高估2%；④ 悲观情景同时下调ROE并上调kₑ至13%——这在危机中是真实共振，作为**压力测试合理**，但不是概率中性的合理价值。

### 胜宏科技H股

- 证券口径固定为 `02476.HK`；基本面使用胜宏科技合并报表（A股代码300476，2011—2026Q1财务序列取自东方财富F10，2026-08-02）。
- **本标的的核心矛盾：预测利润与历史中周期参照利润相差2.6倍。** 这不是模型缺陷，而是强周期重资本公司在景气高点的真实状态。任何单一"合理价值"都会掩盖它，因此本标的**不使用单一五档估值标签作为结论**。
- **2026-08-02 命名更正**：原"中周期正常化利润"更名为**历史中周期参照利润（升级前产品结构）**——它用的是2011–2024年中位数，而公司在该段时期仍处于低端PCB产品结构，不能代表升级后的盈利能力，故不再称"正常化"。原"三法交叉验证"更名为**三种口径的一致性检查（非独立验证）**——三种算法共用同一段2011–2024历史与相互重叠的会计输入（净利率、ROE、ROIC本就由同一套报表推导），因此只是口径一致性检查，不是三个独立证据源。

**盈利口径四层（必须分开引用，不得混用）**

| 口径 | 金额 | 推导 | 证据等级 | 当前H股PE |
|---|---|---|---|---|
| 2025已实现归母净利 | 43.12亿元 | 年报审计 | A｜已审计 | 36.3倍 |
| 历史中周期参照利润（升级前产品结构） | 约24.13亿元 | 三口径一致性检查 | B｜历史推算 | 约64.8倍 |
| 升级后中周期参照利润（同业锚） | 40.8亿元 | 沪电股份2020–2023年（AI景气前但已属高端板）净利率中位数16.5% × 胜宏2026E营收247亿 | C｜同业类比 | 约38.3倍 |
| 2026E景气预测利润 | 62亿元 | Q1实数外推 | B｜单季外推 | 25.2倍 |

- **2026E 62亿元有实数支撑**（此前判为"无来源"，该判断已修正）：2026Q1归母12.88亿元、同比+39.8%，营收55.19亿元、同比+28.0%；按2025年Q1占全年21.4%的季节性外推，FY2026归母约60.3亿元。作为**预测**成立，但它不是正常化利润。
- **历史中周期参照约24.13亿元，三口径一致收敛**：① 中周期净利率9.67%×2026E营收247亿＝23.9亿；② 中周期ROE 14.67%×2025末归母权益166.18亿＝24.4亿；③ 中周期ROIC 12.90%×投入资本187.05亿＝24.1亿。中周期口径为2011—2024年中位数（剔除2025景气年）。**三者共用同一段历史与重叠会计输入，是口径一致性检查，不是独立验证。**
- **升级后中周期参照锚40.8亿元**：若2025年的产品结构升级具有结构性，用同业沪电股份"AI景气前但已属高端板"（2020–2023年）的净利率中位数16.5%类比胜宏2026E营收247亿，得到更贴合"升级后成熟状态"的中枢，对应PE约38.3倍——比历史中周期参照（升级前结构，64.8倍）更合适，但仍属同业类比（C级证据），不是独立验证。
- **2025年是极端异常年**：ROE 35.56%、ROIC 24.39%、毛利率35.22%、净利率22.35%，四项均为中周期的2—2.5倍。**"这一跃升是结构性产品结构升级还是周期性景气"是本标的最核心的研究问题**；`05传导监控`页S01–S06已给出分项证据链（见下），但24.4%的ROIC中结构性与周期性各占多少仍无法用现有公开数据拆分。

**结构性证据与周期性风险（05传导监控页 S01–S06，2026-08-02新增）**

| 链ID | 证据方向 | 核心发现 |
|---|---|---|
| S01 订单与客户结构 | ✅结构性证据 | 2025直接出口148.21亿，同比+126.9%；内销44.71亿仅+6.5%；出口占比60.88%→76.83%，增长几乎全部来自海外 |
| S02 产品结构与毛利率 | ✅结构性证据 | PCB制造分部毛利率2024年17.86%→2025年30.89%，核心分部本身跃升，非"其他业务"混合抬升 |
| S03 同业锚定 | ✅结构性证据 | 沪电股份2020–2023毛利率已达27.2–31.0%（AI景气之前），说明高端PCB 30%+毛利率是可持续的结构性水平 |
| S04 持续性 | ✅结构性证据 | 跃升已持续5个季度（2025Q1毛利33.37%→2025全年35.22%→2026Q1 34.46%），非单季异常 |
| S05 扩产与自由现金流 | ⚠周期性风险 | 2025资本开支66.17亿（2024仅8.34亿，增7.9倍），FCF −20.14亿，是后续行业价格压力来源 |
| S06 行业级vs公司级 | ⚠周期性风险 | 沪电ROIC同期由12–13%升至21.24%，与胜宏同向同幅，说明这是行业级景气而非胜宏独有；胜宏15年历史毛利率从未超过27.6% |

**估值方法组合（无单一主模型）**

1. *五年利润路径＋推导终局PE*：起点62亿，中性增速20/15/12/10/8%，终局PE由H模型（稳定ROE 16%、g 4%、k 11.5%、10年淡出）推导约12.4倍，中性五年IRR约−1.4%。**此线的定位是"景气延续情形"，不是中性估值。**
2. *终局PE参照区间*：中周期ROE 14.67%的Gordon值约10.1倍；本表稳定ROE假设约10.4倍；H模型约12.4倍；原手填22倍。成熟期锚集中在**10—13倍**；22倍对应成熟期派息75%时隐含股权成本11.7%，作为"仍在增长阶段"的退出倍数可自洽，但**不是成熟期倍数**。四个都是参照，都不是答案。
3. *反向工程（EV/IC备考口径，对盈利预测依赖最低）*：基准日2026-03-31（IPO前最后一张官方资产负债表）。投入资本＝归母权益174.04亿＋有息负债78.70亿−货币资金37.94亿＝**214.80亿**。**H股IPO（2026-04-21增发9585万股、04-27绿鞋1437.75万股，合计110,227,500股）的募资同时进入权益与现金，因此不改变投入资本，但会一比一压低企业价值**。EV＝市值＋有息负债−现金−IPO净募资P＝1604.76−P。P=0时EV/IC 7.47倍、按WACC 11.2%反解隐含永续增长9.2%（ROIC取2025峰值24.4%）；P=100亿时为7.01倍/9.0%。**IPO净募资金额尚未取得（需港交所配售结果公告或2026年中报），已在表内留作输入格。** 修正前该块误用2025年末资产负债表且未计IPO，得EV/IC 8.48倍、隐含增长9.4%，系高估。
4. *反向指标*：同倍数退出IRR约13.3%；10%年化所需2031退出PE约21.7倍。二者不依赖终局PE假设，但**完全依赖62亿基线**——基线若降至2025实际43.12亿，所需退出倍数由21.7倍升至约31.2倍。故只能称"对终局倍数不敏感"，**不能称绝对稳健**。

**H模型的隐含假设（已在表内显式化）**：恒定派息率使模型隐含淡出期ROE由约32%线性降至16%（g÷留存率25%）。该路径与2025年实际ROE 35.56%、中周期14.67%的区间吻合，但它是模型的**隐含结果而非独立假设**，缺少订单、产能利用率、增量ROIC证据支撑，10年淡出期存在虚假精确风险。H模型对本标的只能作参照——它假设增速线性单调衰减、派息率恒定、且不约束再投资一致性，这三条对强周期重资本企业都不成立。

**结论与置信度**：中｜低。2026-07-31 H股HK$184.40。**结构性证据（S01–S04）**：增长全部来自出口(+126.9%)、核心分部毛利率由17.86%升至30.89%、同业沪电证明高端PCB 30%+毛利率在AI景气前即可持续、跃升已持续5个季度。**周期性风险（S05–S06）**：2025资本开支增7.9倍且FCF −20.14亿、同业ROIC同步同幅跃升说明是行业级景气而非公司独有、15年历史中毛利率从未超27.6%。**综合判断：产品结构升级是真实的，但当前24.4%的ROIC同时含有结构性升级与行业景气两部分，无法用现有公开数据拆分。** 故盈利中枢应看作区间（历史中周期参照24.1亿 ~ 升级后同业锚40.8亿），对应PE 64.8x~38.3x，而2026E预测口径为25.2x。不应把任何单一五档标签当作结论；仓位上限应低于口径清晰的标的。

- 必须持续验证：订单、AI产品结构、产能利用率、良率、毛利率、ROIC与自由现金流。不能用静态TTM PE机械抄底，也不能因PE下降就机械补仓。季度更新条件见`05传导监控`页S01–S06下方的更新机制说明（同时满足4项方可上调长期参数；任一触发即下调）。
- **当前实装位置**：胜宏科技H的盈利口径、估值区间、关键参数和传导证据已迁入 `05_数据仓库/investment_research.sqlite`，由 `data/stocks.json` 提供给 `估值仪表盘.html` 展示。旧工作簿中的对应页面只作历史参考。

## 5. 更新节奏与触发规则

| 周期 | 更新内容 | 是否直接交易 |
|---|---|---|
| 每个交易日 | 三只股票价格、市值/PE/PB等行情；三大市场估值温度 | 否，只更新价格落点和市场背景 |
| 每月 | 检查市场估值温度、危机指标和页面数据新鲜度 | 仅作为复核提醒 |
| 每季度 | 财报事实、EPS/BVPS/ROE/利润率/现金流、五年路径 | 是，若长期假设或安全边际改变 |
| 半年/年度 | 产业地图、利润池、头部公司位置、模型参数 | 只有结构变化才调整长期逻辑 |
| 重大事件 | 监管、技术路线、客户/治理、信用危机、渠道体系永久变化 | 立即判断模型是否失效 |

交易动作必须按以下顺序产生：

> 新数据/事件 → 定位到监控链 → 判断暂时性或结构性 → 更新经营假设 → 重算估值与预期回报 → 检查市场仓位和个股上限 → 买入/持有/减仓

## 6. 自动化与人工边界

已自动化：

- `../update_stock_valuations.py`：抓取三只股票最近交易日行情，写入SQLite，重建`data/stocks.json`与HTML fallback。
- `../update_market_temperature.py`：抓取市场估值温度数据，写入SQLite，重建`data/market_temperature*.json`。
- `../build_mobile_dashboard.py`：从SQLite生成前端数据。
- Codex每日定时任务（工作日18:10）：依次运行个股更新、市场温度更新、JSON/HTML校验；校验通过后检查Git工作区，若有变化则提交并推送到GitHub，触发GitHub Pages自动刷新；若无变化则跳过提交。禁止运行Excel/Word/WPS旧链路，禁止打印大段diff。

日更自动提交规则：

1. 只有在行情/市场数据更新脚本成功、SQLite写入成功、前端JSON和HTML fallback校验通过后，才允许提交。
2. 先看简短Git状态；无文件变化时不提交、不推送，避免非交易日产生空提交。
3. 有变化时提交信息使用类似 `data: update valuation dashboard YYYY-MM-DD` 的格式。
4. `git push` 失败、数据明显异常、数据质量告警需要人工复核时，任务应通知用户；普通成功更新不打扰。
5. Git操作本身不消耗明显模型token；避免输出完整diff和大日志，保持定时任务轻量。

必须人工判断：

- 正常化利润、未来五年增长路径、可持续ROE、终局PE/PB。
- 产业传导是否真正改变长期供需、竞争格局或资本回报。
- 市场仓位与个股仓位之间的最终组合决策。

已废弃日常链路：

- Excel/Word生成脚本；
- WPS同步；
- 从工作簿读取数据生成前端；
- 用工作簿作为唯一正式结论源。

## 7. 当前完成度与下一步

已完成：

- 手机端估值仪表盘。
- SQLite数据仓库。
- 三只股票的实用区间、严格区间、关键参数与人读结论。
- 三大市场估值温度的真实数据展示。
- 价格区间概率（对数正态模型）与市场观点（真实研报一致预期、估值倍数对比）两个模块。
- 个股行情和市场温度的轻量更新脚本。
- GitHub Pages 公开部署，手机可直接访问（见第0节）。

仍需推进：

1. **季度财报入库流程脚本化**：目前日更已经脚本化，季度更新仍需要人工把财报事实和模型参数写入SQLite。
2. **胜宏科技结构性证据继续补强**：重点是产能利用率、良率、增量ROIC、订单能见度和扩产后自由现金流。
3. **A股危机阈值校准**：目前美股/港股已有VIX与HIBOR利差历史校准，A股仍缺可靠信用/流动性压力序列。
4. **积累历史快照**：至少保留四个季度的“原假设—新证据—模型调整—实际结果”复盘记录。
5. **市场估值温度数据质量警告待复核**：A股PB分位抓取值与核验快照偏差8.8%，见第4节"已知待复核项"。

## 8. AI接手约束

- 开始任务前先读本文件和`00_项目总览与使用说明.md`。
- 不从聊天记录中的旧数字直接覆盖数据库；先核对数据日期、来源和字段口径。
- 不为了得到“看起来合理”的结论反推公式或参数。
- 修改估值时，必须同时展示关键输入、计算结果、区间变化和操作含义。
- 不把临时预览、验证文件、过程脚本放进本定稿目录。
- 修改前端或数据脚本后必须检查：SQLite最新记录、JSON输出、HTML fallback、页面是否能读取真数据。

补充约束（2026-08-02 新增，源自本次模型审查）：

- **不得手填终局PE/PB。** 必须由稳定期参数推导；引入可比倍数须同时写入来源。见第4节跨模型统一规则。
- **不得把单年预测称为“正常化”。** 命名必须区分：当期 / 情景基线 / 正常化。
- **任何“市场隐含X”类反推读数，必须与其前提参数同格呈现**（如隐含ROE必须带kₑ与g），不允许单独引用。
- **改动估值参数时，必须同时给出该参数的敏感度或保本反推值。** 若某个结论对单一未经推导的参数高度敏感，应把“保本参数值”作为主要输出，而不是把估值标签作为主要输出。
- 日更脚本不得触碰Excel、Word或WPS云端。
- 同一交易日的`price_only`记录应覆盖；新交易日才新增。
- SQLite是当前唯一机器数据底座；JSON/HTML都应由SQLite生成。
